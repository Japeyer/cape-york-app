# Status — Cape York 2026 PWA

> 🎨 **AKTIVE ARBEIT LÄUFT HIER:** `C:\Users\<user>\Projects\cape-york-app` (außerhalb OneDrive), Branch **`redesign`**. OneDrive-Ordner = eingefrorenes Backup. `main` @ `801ff8f` = live/deployt.

**Letztes Update:** 2026-07-14 (aw) (**Cyber-Security-Audit + Härtung + Push.** App-Sicherheit exzellent: 0 Netzwerk-Calls, 0 XSS-Sinks, keine externen Ressourcen, keine Datensammlung (keine Kundendaten auf Servern — alles on-device). `deploy.yml` gut abgesichert (nur push:main, minimale permissions, kein Fremdcode). Keine Secrets/Tokens je committed, Prod-Deps 0 Vulnerabilities. **Härtung:** CSP (`connect-src 'self'` etc.) nur im Prod-Build via Vite-Plugin injiziert (Dev-HMR bleibt heil); `persist-credentials: false` im Checkout; neutrale Git-Identität (`Cape York Dev <…@users.noreply.github.com>`) für neue Commits. **Offen (Entscheidung nötig):** Git-HISTORY trägt noch private Identität (Author-Metadaten aller 30 Commits + 65 Inhalts-Treffer) → normaler Push entfernt das nicht; Optionen History-Rewrite+Force-Push / Repo privat / frisches Repo / akzeptieren. `premium.js`-SECRET client-seitig (Keys fälschbar, für v1-frei egal). **Gepusht** auf `origin/redesign` (`d82ff74..d3df471`, neutraler Author). 299 Tests grün, Build grün.)

**Letztes Update vorher:** 2026-07-14 (av) (**Privacy Policy: OSM raus + Kontakt-Platzhalter.** In PRIVACY.md standen noch OSM-Abschnitte; die live gehostete Policy (`japeyer.github.io`, läuft von **`main`**) zeigt zudem noch die alte Version inkl. privater Mail — meine Änderungen sind auf `redesign`, **noch nicht gemerged/deployt**. Entfernt: OSM-Absatz in § 4 + ganze § 8 „Open-source data attribution" (ODbL), Sektionen umnummeriert. Kontakt = **Platzhalter `support@example.com`** (2 Stellen, beim Release durch echte Support-Mail ersetzen). `build-privacy.mjs` strippt jetzt HTML-Kommentare → interne Notizen + `japeyer`-URL nicht mehr im öffentlichen privacy.html. `privacy.html` neu generiert, Grep sauber (kein OSM/Identität), 299 Tests grün, Build grün. **Live erst nach `redesign`→`main`-Merge + Deploy.**)

**Letztes Update vorher:** 2026-07-14 (au) (**Alle sichtbaren OpenStreetMap-Verweise deaktiviert.** Passend zur deaktivierten Map. Fuel-Sektion (Haupt-OSM-Konsument) war schon in Release `k` weg; sichtbar blieb OSM nur in About-„Data sources" + einem Disclaimer-Satz. Entfernt: About-„Data sources"-Sektion + ROUTE_POIS-Import in `AboutTab.jsx`; `about.sourcesHeading`/`sourcesBody` + OSM-Satz aus `disclaimerBody` + toter Fuel-String-Block (`fuelAttribution` „© OpenStreetMap…" u.a.) in `strings.js`. **Bleibt für V2:** `S.map.attribution` (rendert nur in der via `MAP_ENABLED=false` deaktivierten Map) + OSM-Datenfiles + Attribution-Konstante/Tests (aktuell nirgends angezeigt → keine ODbL-Pflicht). Grep sauber, 299 Tests grün, Build grün.)

**Letztes Update vorher:** 2026-07-14 (at) (**Security/Identitäts-Scrub vor Play-Store-Release.** Ziel: keine privaten Infos zugänglich, Identität (v.a. private E-Mail) überall weg. Audit: Repo public → App + Policy + Docs zugänglich. **Security:** keine Keys/Tokens/Tracking, aber Lizenz-`SECRET` in `premium.js` liegt im Client-Bundle (Keys fälschbar; bewusst gelassen, für v1-frei irrelevant, als Empfehlung notiert). **Entfernt:** Kontakt-Mail + „View source"-Link aus `strings.js`/`AboutTab`; Premium-Kauf-Mailto (lief an private Adresse) → `checkout.js` `type:'unavailable'` + `PremiumInfoTab` bereinigt; `PRIVACY.md`/`privacy.html` Name+Mail raus (Kontakt neutral via Play-Store-Listing); Repo-Docs via Einmal-Skript gescrubbt (Name→„der Entwickler", E-Mails→`<dev-email>`, `Peyer-Schaer`→`<privat>`, Windows-User→`<user>`), Skript danach gelöscht. **Akzeptierte Rest-Exposition:** `japeyer.github.io`-Hosting + `github.com/Japeyer` bleiben (Username sichtbar; später neutrales Hosting nötig). **Verifikation:** Voll-Repo-Grep sauber, 299 Tests grün, Build grün, privacy.html neu gebaut.)

**Letztes Update vorher:** 2026-07-14 (as) (**Geschätzte Einkaufskosten entfernt.** der Entwickler: Kostenschätzung nicht nötig + verleitet zu falschen Annahmen → raus. Entfernt: `progress-cost`-Block + Cost-useMemos in `ShoppingTab.jsx`, Cost-Footer in `lib/share.js` (formatShoppingListAsText ohne totalCost/remainingCost/formatAud), `S.shopping.cost`-Strings, `.progress-cost*`-CSS. **`lib/pricing.js` + `pricing.test.js` gelöscht** (nur ShoppingTab nutzte sie). `share.test.js` Cost-Tests → „keine Kostenschätzung mehr". **Premium-Kaufpreis (AUD$15.99) bleibt** (separates Feature). **299 Tests grün**, Build grün. Auf `redesign`, nicht gemerged.)

**Letztes Update vorher:** 2026-07-14 (ar) (**Zutaten app-weit vereinheitlicht + can/gram-Bug behoben.** Entwickler-Report: Liste sagte „376 Dosen Tuna". **Root Cause:** `parseAmount('185g can/person')` las 185 (Gramm) als Dosen-Anzahl. **Fix generator.js:** Container-Anzahl-Schutz (Zahl direkt vor Masse/Volumen-Einheit = Größe, Anzahl aus „N ×" oder 1) + Regressionstests. **Daten-Normalisierung** via neuem `scripts/normalize-recipes.mjs` (auditierbar, Dry-Run): **173 Änderungen, 14 entfernte Optionals.** Einheitliche Einheit pro Zutat (26 vorher gemischt): Butter/Bacon/Mehl/Hummus/Pinienkerne/Spinat/SourCream→g, Öl/Zitrone/Limette→tbsp, Honig/Ahornsirup→ml, Ingwer/Aubergine/Tomate/Frühlingszwiebel/Pak choi→Stück, Kräuter→tsp, Kirschtomaten/Hähnchen→g. Alle Konserven führen „can"; **`packs.js` erweitert** (Bohnen/Kichererbsen/Mais/Erbsen/Tuna/Salmon mit `contains`) → g/ml/Dosen mergen zu ganzen Dosen (Tuna jetzt 6 statt 376, Kokosmilch 11 Dosen ohne ml/can-Mix). **111 „X or Y" aufgelöst** (Allergen-/Fleisch-/Shelf-Tokens erhalten). **13 Schritte/Tipps** nachgezogen (keine Referenz auf entfernte Zutaten). Neue Konsistenz-Tests (keine Konserve als Stück, kein „or", eine Einheiten-Klasse pro Zutat, Tuna-Regression). **315 Tests grün** (+8). Build grün. Waste-Test ≤2→≤3 Carbs (Folge korrekter „Rice noodles"-Auflösung). Auf `redesign`, nicht gemerged.)

**Letztes Update vorher:** 2026-07-13 (aq) (**Erststart leer + Onboarding-Tutorial beim ersten „Create trip".** Entwickler-Wunsch: Erststart ohne Voreinstellungen → leere Startseite; erster „Create trip" → kleines Tutorial. **(1) Leerer Erststart bereits erfüllt/verifiziert:** `loadTripStore()` gibt bei frischem Install `{trips:[],activeTripId:null}` → HomeTab-Empty-State; kein Default-Trip geseedet, nichts persistiert bis User handelt (kein Code-Change). **(2) Tutorial neu:** Komponente **`TutorialOverlay.jsx`** (Portal-Modal, z-index 1100, 6-Slide-Carousel: Welcome→Configurator→Menü/Swap→Offline-Rezepte→Einkaufsliste→Stock, Dots+Back/Next+Skip). `useStorage`: `getTutorialSeen`/`setTutorialSeen` unter globalem `ui_tutorial_v1` (überlebt Trip-Reset → nur einmal). `App.jsx`: `handleCreateNew` in `doCreateNew`+Gate aufgeteilt (Limit→Tutorial-first→anlegen), `handleTutorialDone` markiert gesehen + legt an; Overlay gerendert. Strings `S.tutorial` (slides[]), CSS `.tut-*` (Warm-Design). **307 Tests grün** (unverändert). Build grün. Re-Test: `localStorage.removeItem('ui_tutorial_v1')`. Auf `redesign`, nicht gemerged.)

**Letztes Update vorher:** 2026-07-13 (ap) (**Kochaufwand-Filter im Configurator.** Entwickler-Wunsch: Schaltfläche „Kochaufwand — wenig/mittel/mehr", dann nur passende Rezepte. Neues Config-Feld **`cookEffort`** (`low|medium|high`) als **kumulative Aufwands-Obergrenze**: low=nur `effort:'easy'`, medium=easy+medium, high=alles. Rezepte trugen das `effort`-Tag schon (62 easy/34 medium/2 hard), Generator nutzte es bisher nicht. **Default `high`** = kein Filter = bisheriges Verhalten (rückwärtskompatibel). Neuer exportierter Helfer `effortAllowed()`; `cookEffort` durchgereicht durch `buildRecipePool`/`buildSplitPool`/`generatePlan`/`computeSpecialAssignments`/`estimateSpecialCount`/`generate` → filtert regulären Pool **+ aufwändige Specials**. **Aufwands-Fallback vor Diät-Fallback** (Diät bleibt hart). config trägt jetzt `cookEffort`+`cookEffortApplied`. **Swap + manuelle Overrides bleiben ungefiltert** (bewusste Einzel-Wahl möglich). UI: neuer PillPicker „Cooking effort" (Easy/Medium/More) im ConfiguratorTab nach Diät; `defaultConfig` + App.jsx generate-Deps erweitert; Strings `effortLabel`/`effortHint`/`effortOptions`. **307 Tests grün** (+11). Build grün. Auf `redesign`, nicht gemerged.)

**Letztes Update vorher:** 2026-07-13 (ao) (**Schrift app-weit auf Calibri-Stil + Map fürs Release deaktiviert.** Entwickler-Wunsch: (1) „gesamten Text = gleiche Schriftart, sauber/klar wie Calibri, Hierarchie nur über Größe/Weight". **Manrope verworfen → Carlito gebündelt** (metrik-kompatibler Calibri-Klon, `@fontsource/carlito`, OFL; nur Latin 400+700 importiert in `main.jsx`). Familie zentral über `--font-ui` in `index.css` = `'Carlito', Calibri, 'Segoe UI', system-ui, …`; Body + `--display` (Titel) nutzen dieselbe Familie, Hierarchie bleibt über Größe/Weight (Titel 700). **Offline gebündelt → geräteübergreifend identisch** (Windows/Android/iOS), ~4× 30 KB (kleiner als Manrope). **`@fontsource-variable/manrope` deinstalliert** + Import raus; **`@fontsource/carlito` neu**. Mono bleibt nur für den Lizenzschlüssel (Code-Token). (2) **Map deaktiviert** für ersten Release (noch nicht gebraucht): neues Modul-Flag `MAP_ENABLED = false` in `App.jsx` blendet Map-Tab (Bottom-Nav) + 🗺️-Topbar-Button aus; **Code + Daten (`InfoMapTab`/`CapeYorkMap`/Geo/POIs) bleiben vollständig** — Reaktivierung = Flag auf `true`. **296 Tests grün** (unverändert), Build grün, Bundle kleiner (Manrope-woff2 raus). Auf `redesign`, nicht gemerged.)

**Letztes Update vorher:** 2026-07-09 (an) (**UI-Redesign v2: „Warm Claude" Item-Design + app-weite Schrift.** Phase-1-Reskin (cool weiß, `c38edce`) gefiel der Entwickler nicht → revertiert (`02cd93c`, == altes Design). Neu: **Items** im warmen Claude-Look (nur CSS/Markup, **keine Logik**, 296 Tests grün). **Schrift app-weit = Manrope** (`@fontsource-variable/manrope`, offline, variabel 200–800) als EINZIGE Familie für Body + Titel; Hierarchie nur über Größe+Weight (Titel 700, Body 400). Font-Iteration: Fraunces-Serif → Space Grotesk-Display (beide vom Entwickler abgelehnt) → Manrope („konsistent, modern, nicht over-the-top"). `index.css`: additive Warm-Tokens (`--card` Papier-Weiß, `--card-line`, `--tok-bg`, `--chip-bg`, `--display`, `--shadow-item`). Recipe-/Day-Cards: warmes Papier + weicher Schatten + Hairline + Radius 18, Emoji in rundem getöntem **Token** (46px), **Manrope-Bold-Titel**, Recipe-Meta als **Chips** (`.meta-chip`). Chrome (Topbar/Nav/BG) bleibt das alte warme Design. **Offen (nächste Iteration):** Item-Sprache auf Shopping-/Inventory-/Cat-Cards ausrollen, optional Meal-Type-Farben, Motion. Auf `redesign`, nicht gemerged.)

**Letztes Update vorher:** 2026-07-07 (al) (**Eigene Rezepte („Build your own menu").** Entwickler-Wunsch: Rezepte im Recipes-Tab anlegen → im Day-Planner per Swap platzieren → Zutaten fließen in die Einkaufsliste des Tages. Frei für alle, globale Bibliothek, hand-platziert (nicht Auto-Round-Robin). Neue **`src/lib/recipe-pool.js`** (`setUserRecipes`/`recipeById`/`isUserRecipe`) — Generator + `inventory.js` nutzen `recipeById` statt statischem `RECIPE_BY_ID`; Auto-Pool-Builder bleiben auf eingebauten RECIPES (User-Rezepte NUR per Swap). Globale Bibliothek `user_recipes_v1` in `useStorage.js` (get/save/upsert/delete). `App.jsx`: `userRecipes`-State + Registry-Sync im Render + `userRecipesHash` in generate-Deps + save/delete-Handler (Delete räumt dangling overrides). UI: `RecipesTab` „👨‍🍳 My recipes"-Sektion (Edit/Delete + „+ New recipe") + portaled `RecipeEditorSheet` (Name/Emoji, Meal-Type/Diät/Burner-Pills, dynamische Zutaten, Steps); `SwapSheet` „My recipes"-Sektion (ohne Diät-/Burner-Filter) → Pick=Override→Shopping. **296 Tests grün** (+8). Build JS →**1319.41 kB**.)

**Letztes Update vorher:** 2026-07-07 (ak) (**Multi-Trip: Premium kann mehrere Trips speichern.** Free = 1 (2. → Upgrade), Premium = unbegrenzt, jeder Trip mit editierbarem Namen. **Store** `cfg_trips_v1` = `{trips:[{id,name,config}], activeTripId}` in `useStorage.js` mit reinen Helfern (migrate/create/delete/rename/setActive/getActive/putActiveConfig) + `mergeConfig` (aus altem loadConfig extrahiert). **Per-Trip-Namespace** für alle volatilen Keys via `setActiveNamespace(tripId)` → `<prefix><tripId>~<rest>`; Consumer unverändert; `ui_` bleibt global; `wipeTripVolatile(id)` löscht 1 Trip; `allStorageKeys()` robust. **Migration**: altes `cfg_v1`→Trip `t1`, bestehende volatile Keys→`…_t1~…` (Checkboxen/Inventar bleiben; per Polyfill end-to-end verifiziert inkl. Isolation). **App.jsx** `store`+`config`(aktiver Trip), Aktionen createNew(limit-gate→Premium)/switchToTrip/open/edit/delete/rename, Store-Refresh bei Home. **HomeTab** = Trip-Liste (Name+✎rename, Summary, Open/Edit/🗑), „+ New trip" mit 🔒 wenn Free am Limit. `FREE_LIMITS.maxTrips=1`. **288 Tests grün** (+9). Build JS →**1312.28 kB**. Lapsed-Premium nicht-destruktiv.)

**Letztes Update vorher:** 2026-07-07 (aj) (**Einkaufsliste: „Wo wird das verwendet?"-Dropdown mit Rezept-Link.** der Entwickler will pro Item sehen, in welchen Menüs die Zutat vorkommt (Rezept-Link) → Wichtigkeit einschätzen. Neue `recipeUsageMap(plan)` in `lib/inventory.js` (kanonischer Key → Rezepte mit `amount`=Rolle + `days`). `ShoppingTab` bekommt `plan`+`onOpenRecipe`, `CheckItem` hat aufklappbaren „🍽 N ▾"-Button → Liste (Icon+Name+Menge+Tage) tap-bar → Rezept. **Rezept-Navigation verkabelt:** `App.openRecipe(id)` (Recipes-Tab + `recipeFocus`), `RecipesTab` `focusRecipeId`/`onFocusHandled` (Card `defaultOpen` + `data-recipe`-Scroll); damit funktioniert auch MenuTabs „→ Recipe"-Link (`onJumpToRecipe`). `CheckItem`-Layout → flex-column + `.chk-item-top`. **279 Tests grün** (UI; usageMap per Node-Smoke: onion→7, garlic→10). Build JS →**1307.71 kB**. Nur für Generator-Zutaten (Items mit `key`).)

**Letztes Update vorher:** 2026-07-06 (ai) (**Neuer „Stock"-Tab: mengengenaues Inventar.** Entwickler-Wunsch: eingekaufte (abgehakte) Zutaten als Überblick; beim Kochen werden verbrauchte Mengen abgezogen (1 Apfel Rezept, Bestand 4 → 3). **Modell:** `Bestand = Einkauf − Verbrauch`, mengengenau in Basiseinheiten. Einkauf = Summe `amount` aller abgehakten Shopping-Items (alle Supply-Points, pro kanonischer Zutat); Verbrauch aus als gekocht markierten Mahlzeiten (Cooked=alle, Deviation=nur `usedIng`, unmarkiert/Reste/Restaurant=nichts) → verknüpft mit dem (ag)-Toggle. **Umsetzung:** Shopping-Items tragen jetzt `key`+`amount` (numerisch, neben Pack-`qty`); Parse-Helfer exportiert; neue **`src/lib/inventory.js`** (`consumedByCooked`/`subtractAmounts`/`isDepleted`/`formatAmount`, gleiche Parse-Logik → 100% konsistent); neuer **`InventoryTab.jsx`** (In-stock nach Kategorie + „Used up"-Sektion + Empty-State); **📦 „Stock"-Tab** in Bottom-Nav (nach Recipes). **Manueller Verbrauch** (Snacks außerhalb des Menüs): „−1" (zählbar), „✕" (aufgebraucht), „↺" (restore); `invu_<key>`. **„+ Add an item"**: eigene Items (nicht aus Einkaufsliste, Kategorie „🛒 Grabbed on the road"), Persistenz `invadd_<id>`, key `custom:<id>` → −1/✕/↺ + 🗑 Delete; `getAddedInventory`/`setAddedInventory`/`removeAddedInventory`. Rest = Einkauf − Koch-Verbrauch − manueller Verbrauch. **279 Tests grün** (+7, u.a. „alle gekocht → Rest exakt 0"). Build JS →**1305.62 kB**. Follow-up offen: exakte Bedarfsmengen (nicht Pack-gerundet), Koch-Verbrauch nur bei explizit markierten Mahlzeiten.)

**Letztes Update vorher:** 2026-07-06 (ah) (**Meal-Status-Sheet: Redesign + Off-Screen-Bugfix + Accessibility.** Bug: Save-Button hinter der Bottom-Nav. **Ursache Stacking-Context:** `.content` ist `position:fixed` (eigener Context) → das darin gerenderte Sheet konkurriert nur intern, die Nav (`z-index:200` im Root) liegt drüber; z-index-Erhöhen half nicht. **Fix: `createPortal` nach `document.body`** → Sheet im Root-Context, `z-index:1000` schlägt die Nav. Zusätzlich `.ms-sheet max-height:88dvh` (Fallback 88vh), Flex-Column + scrollbarer `.ms-body` + Sticky-`.ms-foot` gegen Überlauf. Redesign: Drag-Handle, Header mit Rezeptname + ✕-Close, Slide/Fade-Animation, 52px-Options-Buttons (Cooked grün/Deviation orange), Checkliste 48px-Rows mit custom 26px-Häkchen-Boxen. A11y: `role=dialog`/`aria-modal`/`aria-label`, `aria-pressed`/`aria-expanded`, Escape schließt. Mark-Button „✎ Log" (Orange-Tint) statt gestrichelt. **272 Tests grün** (UI-only). Build JS →**1298.46 kB**.)

**Letztes Update vorher:** 2026-07-06 (ag) (**Meal-Status verfeinert: „Cooked" vs. „Deviation" mit Zutaten-Checkliste.** der Entwickler: Grund der Abweichung egal — nur „cooked as planned" ODER „Deviation" mit Ankreuzen WELCHER Zutaten verwendet wurden; nichts angehakt = gar nicht gekocht. Der grobe 4-Wege-Status aus (af) wird zutaten-genau: `mealStatus[day][slot]` = `'cooked'` (alle Zutaten, nur Häkchen) ODER `{ used:[idx…] }` (nur diese Rezept-Zutaten-Indizes; leer = gar nicht gekocht); fehlt = Default (wie geplant). `generatePlan` hängt `reviewed`/`deviation`+`usedIng` an; `generateShopping` filtert Zutaten per Index + skaliert `batch`; `isShoppableIngredient` exportiert; `noteCooked` überspringt nicht-gekochte. UI `MealStatusSheet` neu: „✅ Cooked as planned" + „✎ Deviation" mit aufklappender **Zutaten-Checkliste** (shoppable, leer startend, „tick what you used") + Live-Zähler + Save/Clear; Badges ✓ Cooked / ✎ Deviation / ⤫ Not cooked (gedimmt). Checkliste via `RECIPE_ING_INDEX`. **272 Tests grün** (+2). Build JS →**1297.53 kB**.)

**Letztes Update vorher:** 2026-07-06 (af) (**Plan-vs-Reality: Leftovers, Meal-Status, Multi-Stop-Frisch-Einkauf.** der Entwickler im Trip: Plan zu starr — nicht jede Mahlzeit gekocht (auswärts/improvisiert/Reste) → Über-Einkauf, Waste auf Frischem. Drei Fixes (AskUserQuestion) + Ground-Truth: nur verlässliche Supermärkte führen Frisches (Cooktown/Coen/Bamaga/**Weipa** neu), Roadhouses (Archer) nicht. **A1 Reaktiver Meal-Status:** config `mealStatus:{[day]:{f|m|ab:'eaten-out'|'skipped'|'leftovers'}}`, `setMealStatus` in App.jsx, `generatePlan` hängt `meal.status` an, `generateShopping` überspringt markierte Mahlzeiten (Zutaten+Kosten fallen aus Rest-Liste); UI `MealStatusSheet`+„⋯ Mark"+Badge/Dim in MenuTab. **A2 Leftover-aware:** `leftovers:true` auf 15 Dinner; Generator paart ~1×/5 Tage ein Großansatz-Dinner (`batch:1.6`) mit Rest-Lunch am Folgetag (`{leftover:true,from,fromDay}`, kein Einkauf); Shopping skippt Leftover-Slots + skaliert batch. **B Multi-Stop-Routing+Weipa:** `regions.js` `reliableFresh`-Flag + neuer Supply-Point Weipa (Woolworths, ~830km, teal Marker); `generate()` nimmt `stopDays`, baut `freshStops`=Cairns+aktivierte verlässliche datierte Stops; `generateShopping` N-Bucket-Routing (Frisch→letzter Resupply mit day≤Koch-Tag, Haltbares immer Cairns); Roadhouses nur Essentials; ConfiguratorTab stop-State datengetrieben (`OPTIONAL_STOP_IDS`, Weipa automatisch im Kalender). Bamaga-only = altes Verhalten (backward-compat). **270 Tests grün** (260+10). Build JS →**1296.26 kB** (+3.8 kB). Follow-up offen: präzise Per-Leg-Wasser/Eis-Skalierung; `batch:1.6` Heuristik.)

**Letztes Update vorher:** 2026-07-06 (ae) (**Gewürze auf Ultra-Minimal-Kit reduziert.** der Entwickler: „niemand hat Platz für 30 verschiedene Gewürze." ~40 Gewürznamen ≈ 30 Gläser → per AskUserQuestion **Ultra-Minimal-Kit (~6)** gewählt. **Kern:** Salt · Black pepper · Cumin · Smoked paprika · Chili flakes · Curry powder · Mixed dried herbs (+ „Salt & pepper"-Combo). Neues Skript **`scripts/reduce-spices.mjs`** (arbeitet auf geparster Struktur, Bracket-Matching pro `ing:[…]`, ersetzt nur geänderte Blöcke; Substitution + Removal + Dedup-innerhalb-Rezept). Alle Paprika→Smoked paprika; alle Chili-Trockenformen→Chili flakes; Turmeric/Garam masala/Ground coriander/**alle Currypasten**→Curry powder; alle getrockneten Kräuter→Mixed dried herbs; Spezialsalze→Salt. **Gestrichen:** Cinnamon, Vanilla, Nutmeg, Bay leaf, Garlic powder (frischer Knoblauch da), Onion powder (frische Zwiebel), Mustard powder/seeds (Dijon bleibt). **Ergebnis: Trocken-Gewürz-Gläser ~30 → 5; 55 Rezepte angepasst; unique Namen 368→329; keine Doppel-Zutat.** 260 Tests grün, Build JS →**1291.54 kB**. Tradeoff bewusst: Frühstücke (kein Zimt/Vanille) + Thai-Gerichte (Curry powder statt Paste) flacher; Step-Texte referenzieren teils noch alte Gewürze (Politur optional später).)

**Letztes Update vorher:** 2026-07-06 (ad) (**Zutaten-Namen im Rezept-Datensatz vereinheitlicht.** der Entwickler wollte die Quelldaten sauber (nicht nur die Runtime-Kanonisierung aus (ac)): jede Zutat soll in jedem Rezept gleich heißen und nur EINMAL gelistet sein (Beispiel: drei Maple-Syrup-Schreibweisen). Neues Einmal-Skript **`scripts/normalize-ingredients.mjs`** mit expliziter `ALIASES`-Map (exakt alt→neu), Text-Replace nur auf `['<name>',` (Zutat = erstes ing-Element) via Literal split/join — trifft nie Rezeptnamen/Steps; Dry-Run-Report + Zero-Match-Warnung. Alle 426 rohen Namen inventarisiert und gruppiert; nur echte Dubletten/Synonyme/Plurale vereinheitlicht, **genuin verschiedene Zutaten getrennt** (Cuts, Reissorten, Paprika-Typen). Beispiele: Plurale→Singular (Carrots→Carrot, Tomatoes→Tomato, Egg→Eggs, Spring onions→Spring onion …); Synonyme (Bell pepper→Capsicum, Feta cheese→Feta, Milk/UHT whole milk→UHT milk, Beef mince→Ground beef, Aubergines→Eggplant, Tortilla wrap(s)→Tortilla wraps, Fresh ginger→Ginger, Plain flour→Flour); semantisch sauber getrennt: Koriander-Kraut (→`Coriander leaves`) ≠ gemahlen (→`Ground coriander`), aber Cumin/Turmeric nur eine Form (Ground cumin→Cumin); Pepper→Black pepper; Cuts konsistent (Chicken breasts→Chicken breast, Chicken thigh fillets→Chicken thighs). **Ergebnis: eindeutige Namen 426→368, Shopping-Keys 300→262, keine Doppel-Zutat innerhalb eines Rezepts.** Betrifft jetzt auch die Rezept-Ansicht. **260 Tests grün** (Allergen-/Kategorie-/Fleisch-Erkennung laufen auf den Namen → keine Regression). Build JS 1293.30→**1292.46 kB** (−0.8 kB). Runtime-`canonicalIngredient` bleibt Sicherheitsnetz (Reis/Pasta-Familien + Parenthetik + „X or Y").)

**Letztes Update vorher:** 2026-07-06 (ac) (**Waste-Optimierung — pack-bewusste Menü- & Einkaufsgenerierung.** der Entwickler hat den 16-Tage-Eigen-Trip (Juni 2026) beendet → Stufe-2-Unblock. Zwei Praxis-Probleme, beide im Generator: (1) dieselbe Grundzutat als viele unterschiedlich benannte Zeilen (Jasmine/Basmati/Long-grain rice; Spaghetti/Penne/Macaroni), (2) Menü-Vielfalt zwang zum Kauf vieler Groß-Packs, je nur teils genutzt (AU-Supermärkte = nur große Packs) → Foodwaste. **Entscheidung mit der Entwickler:** Menü-Vielfalt behalten, aber Generator pack-fill/ingredient-sharing-aware, mit gelegentlichen gedeckelten Wiederholungen (nie Folgetage, nur um offene Verderbliche aufzubrauchen); Ziel Trocken-Grundzutaten + Verderbliche; **silent Generator-Default, keine neue Configurator-Pille** (Filter-Sprawl vermeiden). **Phase 1:** `canonicalIngredient()` in `generator.js` fasst austauschbare Varianten zu EINER Zeile zusammen — zwei Ebenen: (1) `CANONICAL_RULES` Regex für Familien (Reissorten → `Rice`, Nudel-Formen → `Pasta`, plain `Onion`; Arborio/Sushi rice, Rice noodles/vinegar/flour, Gnocchi getrennt); (2) **generisch** (kein Hardcoding): Trailing-Qualifier (`, grated`/`to serve`) + `"X or Y"` → primäre Zutat, mit Adjektiv-Guard (`Olive or coconut oil`/`Rice or hokkien noodles` bleiben ganz). Fixt den User-Report „drei Maple-Syrup-Varianten" → eine Zeile. Betrifft nur die Einkaufsliste. Neue Datei **`src/data/packs.js`** (`PACK_SIZES`, ~20 Zutaten, `mainStaple`/`perishableOpen`/`contains`). `describeQty()` (ersetzt `formatTotals()`) rundet Masse/Volumen auf ganze Packungen (`1 × 1kg bag · ~900 g used`), Dosen auf ganze (`1 can · uses ~0.5 cans`), führt gemischt notierte Gebinde zusammen (Kokosmilch `500 ml + 1 can` → `3 cans`). `ingredientKey()` raus. **Phase 2:** Score-Sekundärsortierung INNERHALB der gefilterten Kandidaten (`wasteScore`/`chooseWaste`/`noteCooked`, `WASTE`-Konstanten) — Fleisch-Cluster/Specials/Overrides/Restaurant bleiben primär, Pack-Fill ist Tie-Break; Reward fürs Aufbrauchen offener `mainStaple`/Verderblicher, Strafe fürs Anbrechen neuer Grundzutat; Mehl/Zucker/Linsen nur Pack-Rundung ohne Auswahl-Bias. Wiederhol-Regel gedeckelt + nie Folgetage, Determinismus erhalten. `pricing.js` `qtyMultiplier()` parst neues `"N × size"`-Format. **Impact Default 16d/2P/omnivore:** Reis/Pasta/Maple syrup je EINE gerundete/zusammengefasste Zeile, Haupt-Carbs von 4 auf 2 konsolidiert, Kokosmilch als ganze Dosen. **260 Tests grün** (241 + 19 neue). Build JS 1289.23→**1293.30 kB** (+4.1 kB · gzip 390.55 kB), CSS unverändert. Bewusst NICHT: keine Rezept-Edits (97 bleiben; Staple-Erkennung aus Zutaten-Strings), keine Kosten-Rekalibrierung (wartet auf echte Ausgabezahlen).)

**Letztes Update vorher:** 2026-06-01 (ab) (Neue Präferenz „🧄 Garlic" als 9. Allergen/Präferenz-Filter. User-Anfrage: „ergänze noch eine Diät-Präferenz von no garlic". **Pool-Analyse zuerst:** 47/97 Rezepte (48%) enthalten Knoblauch, beim Dinner sind es 40/50 (80%). Strikter Core-Filter würde den Dinner-Pool auf 10 schrumpfen → unbenutzbar. **Lösung: Special-Topping-Default-Regel** — Garlic wird per Default als „topping" (= weglassbar) klassifiziert, weil Knoblauch in fast jedem Rezept (Curry, Pasta, Stir-Fry) eine kleine Aroma-Zutat ist die ohne Qualitätsverlust weggelassen werden kann. **Ausnahme „core"** wenn Garlic im Rezeptnamen primäres Aroma signalisiert (Garlic toast, Garlic shrimp, Pasta aglio e olio). Spezielle `recipeAllergenStatus`-Logik in `allergens.js`: bei `allergen === 'garlic'` Name-Match-Check zuerst → core wenn `GARLIC_CORE_NAME_RX.test(recipe.name)`, sonst topping wenn überhaupt vorhanden. **Pool-Impact:** Frühstück 19/19 bleiben (3 mit Hint), Lunch 28/28 bleiben (4 mit Hint), Dinner 40/41 bleiben (32 mit Hint; nur a24 Pasta aglio e olio als core ausgeschlossen — korrekt). **ALLERGENS-Array von 8 auf 9 erweitert.** Configurator-Pill kommt automatisch (UI iteriert über `ALLERGENS`-Array). `S.config.allergenOptions.garlic = { label: 'Garlic', sub: 'Leave out — recipes still work' }` — Hint im Sub-Label macht die Topping-Semantik transparent. **+6 Tests in `allergens.test.js`:** Knoblauch in Zutaten → topping, kein Knoblauch → kein Eintrag, Garlic toast / Aglio e olio / Garlic shrimp → core, kombiniert mit anderen Allergens. ALLERGENS-Konstante-Test von 8 auf 9 angepasst. **241 Tests grün** (vorher 235, +6). Bundle JS 1288.90→**1289.23 kB** (+0.33 kB), CSS unverändert. Bewusst: Topping-Default statt Core-Default war die einzige praktikable Wahl ohne den Pool unbrauchbar zu machen; Memory `feedback_generator_over_user_filters` warnt zwar vor Filter-Sprawl, aber 9. Pill ist gerechtfertigt als echte häufige Präferenz (Low-FODMAP/IBS/Knoblauch-Aversion).)

**Letztes Update vorher:** 2026-06-01 (aa) (Sequential-Cooking-Support für 1-Burner-Setups. User-Designfrage: „1-Burner Setups sollten auch Menüs kochen können die mehrere Pfannen verwenden, sequenziell — ohne das Burner-System redundant zu machen". **Konzept:** neuer optionaler Tag `parallel: true` pro Rezept für die wenigen die WIRKLICH gleichzeitige Hitzequellen brauchen (Stir-Fries mit Wok-Tip, Risotto mit Mantecatura). Default = sequenzierbar. **4 Rezepte als `parallel: true` getagged:** a7 Chicken stir-fry Bamaga, a20 Tofu stir-fry, a43 Wild mushroom risotto (Special), a45 Thai basil chicken stir-fry. **Generator-Änderung:** neue exportierte Helfer `fitsBurnerSetup(recipe, userBurners)` und `needsSequentialCooking(recipe, userBurners)`. `compatibleRecipesForCat`, `buildRecipePool`, `estimateSpecialCount`, `computeSpecialAssignments` nutzen jetzt `fitsBurnerSetup` statt strict `<=`. `mealEntry()` akzeptiert optional `userBurners` und setzt `out.seq = true` als UI-Marker für Multi-Pan-Rezepte die in 1-Burner-Setup laufen. **UI:** `🕐 Cook in sequence`-Pill (helles Cyan-Grau `#EEF4F8`, etwas dezenter als `tag-special` und `tag-ovr`) in MenuTab-MealRow und SwapSheet-Card, mit tooltip-Hint via `S.menu.sequentialHint`. **Pool-Impact für 1-Burner-User:** omnivore-Dinner ~10 → **46** (29 davon mit Sequenz-Hint), vegan-Dinner 6 → **15**, vegetarian-Dinner 4 → **25**. Burner-System bleibt bedeutsam: 1-Burner-User sehen weiterhin NICHT a45 Thai-Wok-Chicken (parallel:true), und Stir-Fries/Risotto bleiben den 2+/3-Burner-User vorbehalten. **Tests:** 234 → 235 grün (1 Test-Update von Pool-Aware-Special-Coverage `1-Burner liefert 0` zu `1-Burner sieht sequenzierbare Specials = 2`, weil a36/a37/a42 jetzt sequenz-tauglich verfügbar sind; +1 neuer Test der explizit prüft dass a43 Risotto bei 1-Burner NICHT sichtbar ist aber a39 Halloumi sequenz-OK ist). Bundle JS 1288.29→**1288.90 kB** (+0.6 kB), CSS 41.87→**42.01 kB** (+0.14 kB · gzip 7.78→7.82 +0.04 kB). Bewusst NICHT gemacht: kein Configurator-Toggle „Sequential cooking OK" — Memory `feedback_generator_over_user_filters` (Filter-Sprawl vermeiden); die parallel-only Detection läuft im Generator transparent.)

**Letztes Update vorher:** 2026-06-01 (z) (Specials im SwapSheet sichtbar machen. User-Report: „ich kann die special menus im swap immer noch nicht finden, woran liegt das?". **Diagnose:** Specials waren tatsächlich im Swap-Sheet enthalten (`compatibleRecipesForCat` filtert nicht über `occasion`, nur Diät/Burner/Allergens — bei 2-Burner-omnivore zumindest a42 Beef stroganoff sichtbar), aber **als normale Cards gerendert ohne visuelle Markierung** → unterhalb 17 anderer Dinner-Optionen unsichtbar. **Fix in `SwapSheet.jsx`:** (1) Kandidaten in zwei Gruppen aufgeteilt: `specials` (r.occasion === 'special') + `regulars`. (2) Specials werden ZUERST gerendert mit Section-Header `✨ Special occasion dinners`, dann Header `All recipes` + reguläre Karten. Wenn 0 Specials passen, kein Section-Header (kein verwirrender leerer Block). (3) Special-Cards haben warm-orange Background (`#FFF7ED`, gleicher Style wie `.cfg-special-hint` aus Configurator-Preview) und einen Inline-Badge `✨ Special` neben dem Rezeptnamen. **Strings:** `S.menu.swap.specialBadge`, `specialSection`, `regularSection` (en MVP, i18n-ready). **CSS:** neue `.swap-section-header` (11px uppercase + bottom-border), `.swap-card-special` (orange Variante mit active-state), `.swap-card-badge` (orange Pill 10px). **234 Tests grün** (unverändert — UI-only-Änderung, kein Generator-Code). Bundle JS 1286.74→**1288.29 kB** (+1.55 kB), CSS 41.33→**41.87 kB** (+0.54 kB · gzip 7.71→7.78). User sieht jetzt bei jedem Dinner-Tap im SwapSheet die für sein Setup verfügbaren Specials sofort oben mit Badge.)

**Letztes Update vorher:** 2026-06-01 (y2) (User-Wunsch-Rezept eingearbeitet: **a50 Pan-fried sweet potato fries with veg and your choice of meat** — der Entwickler: „süsskartoffel stäbchen in der pfanne gebraten mit pfannen gemüse (brokoli, peperoni, zwiebeln) mit einem beliebigen stück fleisch (250g pro person) und einer creme fraiche dip der mit salz und zitronen konzentrat gemacht wird". Omnivore, 2-Burner, 30 min, `effort:'easy'`, `cooling:'high'`. Designentscheidungen: (1) **„Beliebiges Fleisch" als Flexibilität verankert** statt 4 separate Rezepte — Zutaten-Liste sagt „Your choice of meat (steak, chicken breast, lamb cutlets, or pork loin)", `containsFreshMeat` matcht über generisches „meat" + Cuts-Token. Tip empfiehlt steak/lamb für Cluster-Day-1-2 (höchster Wert zuerst), chicken/pork für Day-3+. (2) **2-Burner-Choreographie:** Pan 1 = Sweet-Potato-Fries 14–16 min (langest), Pan 2 = Gemüse → dann Meat sear in derselben Pfanne (Veggies zur Seite). (3) **Crème-fraîche-Dip kalt prepared first** (Aromen vermählen), Tip nennt PJ's/Spiral-Foods Lemon-Concentrate als shelf-stable. (4) **350g Süsskartoffel/Person** üppig (Süsskartoffel verliert ~30% Volumen beim Frying). (5) **„Don't stir for 4 min"-Crust-Trick** im Step 2 — sonst matschig statt knusprig. Allergie-Profil: dairy (crème fraîche), GF, nut-free, soy-free, egg-free. Pool 96 → **97 Rezepte**. Test `pool-Gesamtgröße` von 96 auf 97 angepasst. **234 Tests grün** (unverändert). Bundle JS 1285→**1286.74 kB** (+1.7 kB · gzip 387.79→388.27 · +0.5 kB). Bewusst kein Special-Tag — Round-Robin-Kandidat wie a4/a9; User-Override via Swap-Sheet wenn gezielt platziert.)

**Letztes Update vorher:** 2026-06-01 (y) (Pool-Erweiterung +12 Rezepte mit Allergie-Defensive-Design. User-Anfrage: „vergrössere noch den Pool für alle Menus auch mit diätspezifischen Präferenzen und Allergien". **Pool-Analyse zuerst:** klare Lücken — Zöliakie sah **0 Frühstücke in jeder Diät** (kritisch), Vegan-Lunch+Gluten=0 (kritisch), Vegan-Breakfast nur 7 (dünn), Vegetarian-Dinner nur 7 regulär. **12 neue Rezepte strategisch designed (4 Breakfast, 3 Lunch, 5 Dinner):** **Breakfast:** f16 Coconut chia pudding with mango (vegan, GF/DF/nut-free/soy-free/egg-free — maximal allergen-defensiv, no-burner prep-ahead), f17 Quinoa breakfast bowl with berries (vegetarian, GF, nut-free), f18 Mexican egg and black bean skillet with corn tortillas (omnivore, GF), f19 Sweet potato hash with eggs and avocado (omnivore, GF/DF/nut-free/soy-free). **Lunch:** m26 Greek chicken salad with feta and olives (omnivore, GF), m27 Mediterranean tuna and white bean salad (omnivore, GF, no-burner — shelf-stable masterpiece), m28 Cucumber-avocado sushi rice bowl (vegan, GF mit tamari, nut-free). **Dinner:** a45 Thai basil chicken stir-fry over jasmine rice (omnivore, GF mit tamari), a46 Chicken fajita bowls with avocado crema (omnivore, GF), a47 Chana masala with coconut basmati rice (vegan, GF/DF/nut-free/soy-free), a48 Thai-style vegetable green curry (vegan, GF/DF/nut-free), a49 Greek-style vegetable moussaka with béchamel (vegetarian, 3-burner, mit lentil-tomato-Schicht statt traditional lamb). **Pool 84 → 96 Rezepte (+14%).** Markiert via Block-Header `// ── GLUTEN-FREE / ALLERGY-DEFENSIVE BREAKFASTS/LUNCHES/DINNERS ──` für Auffindbarkeit. **Coverage-Verbesserung dramatisch:** GF-Breakfast: 0/0/0 → **3/1/1** pro Diät, GF-Lunch: 4/1/0 → 6/2/1, GF-Dinner: 16/7/5 → 20/9/7. Vegan-Breakfast: 7→8, Vegan-Lunch: 9→10, Vegan-Dinner: 11→13. Vegetarian-Dinner regulär: 7→9. **+6 Tests in `generator.test.js`:** jede Diät hat ≥1 GF-Frühstück, Vegan-Breakfast ≥8, Vegetarian-Dinner-Pool ≥15 (inkl. vegan-shared), Vegan-Lunch hat GF-Option, alle 12 neuen IDs im Pool mit Pflicht-Feldern (cat/diet/ing/steps), Pool-Total = 96. **234 Tests grün** (vorher 228, +6). Bundle JS 1264.58→**1285.01 kB** (+20 kB · gzip 381.75→**387.79 kB** · +6 kB), CSS unverändert. Cuisine-Vielfalt: Mexican (f18, a46), Mediterranean (m26, m27, a49), Greek (m26, a49), Thai (a45, a48), Indian (a47), Japanese (m28), Middle Eastern (a39). Bewusst NICHT gemacht: keine neuen Configurator-Pills für Diät-Subkategorien — Memory `feedback_generator_over_user_filters` sagt Filter-Sprawl vermeiden; Allergie-Filter ist die bestehende UI-Affordance, der erweiterte Pool macht sie funktional.)

**Letztes Update vorher:** 2026-05-29 (x) (Bug-Fix: Specials waren bei 2-Burner-Setup unsichtbar. User-Report beim Live-Test: „ich kann die Special-Menüs nicht sehen". **Root Cause:** Von den 6 Specials waren **5 mit `burners: 3`** markiert (nur a39 Halloumi mezze hatte `burners: 2`). Bei `burners=2` (häufigster Mietwagen-Camper-Setup) lieferte der Pool: omnivore 0 Specials, vegan 0 Specials, vegetarian nur 1 (Halloumi). Bei `burners=1` immer 0. **Doppelter Bug:** Configurator-Preview-Hint zeigte trotzdem „2 special evening dinners" weil `estimateSpecialCount()` nur Quota+Cluster berechnete, nicht Pool-Verfügbarkeit. **Fix:** (1) **3 neue 2-Burner-Specials** je 1 pro Diät: **a42 Beef stroganoff with creamy garlic mash** (omnivore, 40min, Bistro-Klassiker mit Sour-Cream-Mantecatura, garlic mash mit raw garlic), **a43 Wild mushroom risotto with white wine and parmesan** (vegetarian, 35min, Restaurant-Grade mit dried porcini + mantecatura), **a44 Coconut laksa feast with crispy tofu and noodles** (vegan, 40min, Malaysian street-food mit cornflour-crusted tofu + fried-shallot-topping). Pool 81 → **84 Rezepte**. Marker `// ── 2-BURNER-SPECIALS ──` für Lesbarkeit. (2) **`estimateSpecialCount` Pool-Aware:** akzeptiert jetzt optional `diet`/`burners`/`allergens` und prüft via `RECIPES.some(...)` ob mindestens 1 passendes Special im Pool ist — sonst returnt 0 trotz positiver Quota+Cluster. Wenn die Filter NICHT übergeben werden, bleibt legacy-Verhalten (theoretisches Maximum) — bestehende Tests grün, Backward-Compat erhalten. (3) **ConfiguratorTab** ruft jetzt `estimateSpecialCount` mit allen 5 Filtern auf — der Preview-Hint zeigt die Wahrheit. **+8 Tests in `generator.test.js`:** 2-Burner-omni/veg/vegan finden Specials, 1-Burner→0, dairy-allergy beeinflusst vegan-Pool nicht, 3-Burner jeder Diät ≥1, Backward-Compat ohne Filter, Generator produziert tatsächlich Specials für 2-Burner-Setup jeder Diät (End-to-End). **228 Tests grün** (vorher 220, +8). Bundle JS 1257.28→**1264.58 kB** (+7.3 kB durch 3 neue Rezepte · gzip 379.66→381.75 kB · +2.1 kB gzip), CSS unverändert. Visuelle Hierarchie der Specials: a36-a41 sind 3-Burner-Sunday-Roast-Niveau (Premium-Argument für Big-Camper-Mieter), a42-a44 sind 2-Burner-Bistro-Niveau (Standard-Camper-Coverage) — beide Stufen pro Diät vertreten.)

**Letztes Update vorher:** 2026-05-23 (w) (Share-Funktion für Einkaufsliste — User-Wunsch „eine begrenzt-zeit Online-Einkaufsliste teilen für gemeinsames Einkaufen". **Pushback gegeben:** echte Online-Liste mit TTL + Live-Sync würde Backend brauchen → bricht `CLAUDE.md` § 5 (kein Backend) + Privacy Policy + Play-Store-Data-Safety-Story. **Drei Optionen vorgelegt:** A Text-Share via Web-Share-API · B self-contained Hash-URL · C echtes Backend mit ~$5/Monat. **User wählte A.** **Implementierung:** Neue Datei `src/lib/share.js` mit `formatShoppingListAsText({ data, checked, supplyPoint, totalCost, remainingCost })` — rendert Header („🛒 Cape York shopping — Cairns · 12 / 80 done"), pro Kategorie alle Items mit ✓/☐-Marker und qty, Cost-Footer („💰 Estimate: ~$340 AUD · ~$220 left"), App-Signatur. `tryWebShare({ title, text })` returnt `{ok}|{ok:false, reason}` — capability-Check via `canShare()` wenn vorhanden, optimistic fallback auf Safari, AbortError → `reason:'cancelled'` (kein Toast, kein Fallback-Sheet). `buildWhatsAppUrl(text)` → `wa.me/?text=<encoded>` (Mobile öffnet App, Desktop öffnet WhatsApp Web). `buildMailtoUrl({ subject, text })` mit 1800-Zeichen-Truncation (manche Email-Clients kappen lange mailtos). `copyToClipboard()` mit graceful Fallback. **`ShoppingTab.jsx`:** Share-Button als dritte Pille in der progress-actions-Zeile (zwischen Reset und Check-all, Brand-Orange-Outline-Style `.progress-btn.share`), nur sichtbar wenn `total > 0`. `handleShare()` versucht Web-Share-API; bei `unsupported` (Desktop-Browser) öffnet sich neue Sub-Komponente `ShareSheet` mit drei großen Buttons (💬 WhatsApp, ✉ Email, 📋 Copy) und Erklär-Sub („Send to a co-driver. Their ticks stay on their phone — no sync."). Copy → Toast „Copied to clipboard" 2.2s. **Strings:** `S.shopping.actions.share` (+ Pille-Label), neuer `S.shopping.share`-Bucket (sheetTitle/sheetSub/whatsapp/email/copy/copied/copyFailed/emailSubject). **CSS:** `.progress-btn.share` warm-orange Variante, `.share-list` flex-column gap-10, `.share-btn` 48px Touch-Target mit Border + active-Background, `.share-sub` 13px muted, `.share-toast` fixed bottom-90px, 2.2s-Animation (`share-toast-in` slide-in 0.18s ease-out). **Tests +14 in `share.test.js`:** Header-Rendering, alle Kategorien + Items, ✓/☐-Marker, qty-Suffix-Skip wenn leer, Cost-Footer, Remaining nur bei partial-checked, App-Signatur am Ende, leere Liste, leere Kategorien skippen, WhatsApp/Mailto-URL-Encoding + Truncation. **220 Tests grün** (vorher 206). Bundle JS 1255.22→**1257.28 kB** (+2 kB · gzip ~380 kB unverändert), CSS 41.21→**41.33 kB** (+0.12 kB). Privacy-Story bleibt sauber: **0 Network-Calls aus dem Code** — App formatiert nur Text und legt ihn dem Browser via `navigator.share` oder `mailto:`/`wa.me:`/`clipboard` vor; User entscheidet selbst was wohin geht. Tradeoff der gewählten Variante: **kein Live-Sync zwischen Geräten** — Reisepartner sieht die Liste als Text-Snapshot, eigene Häkchen bleiben auf seinem Handy. Für 2-Personen-Trip (der Entwickler + Reisepartner Juni 2026) reicht das voll — im Supermarkt redet man eh.)

**Letztes Update vorher:** 2026-05-15 (v) (OSM-Camp-Sites integriert nach gescheiterter QPWS-Recon. User wollte ursprünglich „QPWS-Webseite Camping-Sites + Weblinks". **Recon-Befund:** qldspatial.information.qld.gov.au Catalog-Download braucht Login (verifiziert via Playwright DOM-Dump — Detail-Page hat nur „View metadata" Button, kein Download-Element); ISO-XML hat kein `<distributionInfo>`; WFS 499 (auth); ArcGIS-Online QPWS_Points „QPWS Staff Only" license; UQ-Studenten-Mirror DP_QPWS_CAMP_AREAS_P hat keine Lizenz + „Temporary Data" warning. **Pivot zu Weg A:** OSM `tourism=camp_site` — gleiche ODbL-Lizenz wie Roads/Rivers/NPs, schon in unserer Pipeline. **Implementierung:** `scripts/fetch-osm-geo.mjs` Overpass-Query um camp_site (nodes+ways+relations) erweitert. Neue `extractCamps()`-Funktion: filtert auf named, dedupliziert per (Name + 0.01°-grid), rechnet Centroid für ways via avg-of-vertices, für relations via erstes outer-member. Output-Schema `{ id, name, lat, lng, operator?, website?, fee?, capacity? }`. `renderModule` + `cape-york-pois.js` re-export erweitert um CAMPS. **`CapeYorkMap.jsx`:** neue Konstante `OSM_CAMPS` filtert OSM-Camps die innerhalb 0.006° (~660m) eines hand-kuratierten `layer:'camp'`-POIs liegen — die 8 hand-geschriebenen Camps (Eliot Falls, Bramwell, Chili Beach Camp, Lakefield NP, etc.) mit reichen Blurbs haben Vorrang. Neuer Render-Loop für OSM-Camps (nur wenn 'camp'-Layer aktiv = Premium-User), kleinere Marker (r=3.5) als die hand-kuratierten (r=4.5) — visueller Unterschied „kuratiert vs. mass". Tap → onMarkerTap mit synthetischem POI-Objekt; Blurb wird zur Laufzeit aus tags zusammengesetzt (`Operated by ${operator}. Fee applies.` etc.). **233 named camps** extrahiert (von 257 in bbox; 24 unnamed verworfen) — inkl. Cooks Hut, Wallaman Falls, Cape Tribulation Camping, Bramwell Roadhouse, Archer River Roadhouse, Lakeland Caravan Park, Loyalty Beach (Seisia), und vielen Aboriginal-Community-/Bush-Camps. 11 Camps explizit QPWS/QLNP/Queensland-National-Parks getaggt. **206 Tests grün** (von 187 — der Wachstum-Delta kommt aus parallelen Sessions, nicht dieser Änderung), Bundle JS 1205.86→**1255.22 kB** (+49 kB · gzip 365→~380 kB), Geo-File on-disk 978→**1017 kB**. **Cleanup:** Playwright (kurz für Recon installiert, ~250 MB) wieder deinstalliert, Chromium-Cache aus `~/AppData/Local/ms-playwright/` gelöscht. **`vendor/README.md`** dokumentiert die Sackgasse + behält die Manual-Download-Prozedur als Future-Work-Hinweis falls jemand mal QPWS-Daten dazu nehmen will. **Lizenz-Stand:** ODbL via OSM, Attribution schon im AboutTab vorhanden, keine zusätzliche nötig.)

**Letztes Update vorher:** 2026-05-15 (u) (Special-Marker UI — Fortsetzung von (t). Specials waren bisher unsichtbar. User-Wunsch: sowohl im MenuTab als auch im Configurator soll erkennbar sein dass etwas Besonderes dabei ist. **Zwei UI-Touch-Points:** (1) **MenuTab DayCard** — `dinnerEntry.spec = true` (in (t) gesetzt) wird jetzt als „✨ Special"-Pill neben dem Day-Title gerendert. Style `.tag-special` analog zum existierenden `.tag-n` (Bamaga-NEW): gleiche Pill-Geometrie, dezenter Orange-Background `#FFF3E0`. Pill-Reihenfolge: Today > Special > NEW. (2) **Configurator-Preview-Hint** — neuer Block unter dem Calendar-Tip, sichtbar wenn `hasRange && estimateSpecialCount(...) > 0`. Text dynamisch („1 special evening dinner" vs. „N special evening dinners"). Style `.cfg-special-hint` warmer Orange-Background `#FFF7ED`. **Generator-API erweitert:** `specialQuotaForDays(days)` exportiert (vorher privat) — rohe Quota-Schwelle 0/1/2/3 je Trip-Länge. Neue Funktion `estimateSpecialCount({ days, bamagaStop })` kalkuliert echte Anzahl unter Berücksichtigung des Cluster-Caps (max 1 Special pro Cluster). Configurator nutzt das damit der Preview die Wahrheit zeigt — 25d ohne Bamaga = 1 Special, nicht 3 wie Quota suggeriert. **Strings:** `S.menu.tags.special: '✨ Special'`, `S.config.specialHint({ count })` mit Singular/Plural. **+4 Tests:** Quota-Bänder, Estimate-Cluster-Cap, Plan-Output enthält `spec: true` für UI-Anbindung. **206 Tests grün** (vorher 202). Bewusst: keine Configurator-Option zum Specials-Ausschalten — bleibt Generator-Default, User-Agency via Swap-Sheet reicht. Hint nur bei count>0, kein „0 specials" verwirrender Banner bei <5d-Trips.)

**Letztes Update vorher:** 2026-05-15 (t) (Special Occasion Dinners + Generator-Drosselung. User wollte Steak/Grilladen, hat „Budget-Slider"-Idee zurecht fallen gelassen (Pricing zu grob, Configurator schon dicht, User-Agency via Swap reicht). **6 neue Specials (2 pro Diät, alle Dinner):** Omnivore — a36 Ribeye steak + wedges (3-burner premium), a37 BBQ pork ribs + corn + slaw (3-burner Lagerfeuer-Feast); Vegetarisch — a38 Aubergine parmigiana + garlic toast (3-burner italienisch), a39 Halloumi mezze platter mit dips + flatbread (2-burner sharing-feast); Vegan — a40 Jackfruit „pulled pork" tacos (3-burner mit canned jackfruit), a41 Stuffed butternut roast + quinoa + cranberries (3-burner Sunday-roast). Pool 75 → 81 Rezepte. **Neuer `occasion: 'special'`-Tag** auf den 6 Rezepten. **Generator-Logik `computeSpecialAssignments`:** Quota = 0 für <5d, 1 für 5-12d, 2 für 13-21d, 3 für 22+d (gecappt durch Cluster-Anzahl × 1/Cluster). Platzierung in Cluster-Mitte; Pickup-Tag (1) und Dropoff-Tag (days) ausgeschlossen. Special mit Frischfleisch + Mid-Tag nicht in meatAllowedDays → Verschiebung zum nächsten Frischfleisch-erlaubten Tag im selben Cluster (sonst Steak verdirbt). **Slot-Reihenfolge:** Skip > Restaurant > User-Override > **Special** > Round-Robin. User-Override gewinnt vor Special. `dinnerEntry.spec = true` als Marker (für späteres UI-Badge). **Pool-Isolation:** `buildRecipePool` und `buildSplitPool` exkludieren `occasion: 'special'` — Specials blockieren keine regulären Round-Robin-Slots. `compatibleRecipesForCat` behält sie sichtbar (Swap-Sheet darf sie manuell wählen). **Bug-Fix:** a36-Zutat „Ribeye steak" wurde nicht von `FRESH_MEAT_RX` gematcht (kein `beef`-Token). Korrigiert zu „Beef ribeye steak". **+11 Tests:** Quota-Skala (<5d, 10d, 16d, 25d-mit-und-ohne-Bamaga), Diät-Pool-Auswahl (omnivore/vegetarian/vegan), User-Override-Präzedenz, Pickup/Dropoff-Exclusion, kein Spec im Pool für <5d, Steak-im-Cluster-Test. **202 Tests grün** (vorher 191). Bewusst: keine UI-Änderungen, Specials sind Generator-Default ohne Configurator-Pill; `occasion`-Tag ist generisch erweiterbar für Stufe-2-Occasion-Klassen.)

**Letztes Update vorher:** 2026-05-15 (s) (Vegan-Pool +7 — Convenience- & Faux-Meat-Rezepte. User-Wunsch: vegane Rezepte mit Plant-based Convenience-Produkten (vegane Schnitzel, Plant-Cream, Plant-Mince) für mehr Variabilität — bisheriger Vegan-Pool war fast nur Tofu/Hülsenfrüchte/Gemüse. **Pool 68 → 75 Rezepte (+7 vegan):** f14 Vegan sausages + baked beans (1-burner, v2food/Linda McCartney), f15 Plant-bacon + avocado wrap (1-burner, Suzy Spoon/Sunfed), m24 Vegan schnitzel sandwich + slaw (1-burner, Plant Vibes/Fry's), m25 Plant-based BLT + vegan mayo (1-burner), a33 Plant-based bolognese (2-burner, v2food/Beyond/Birds Eye), a34 Vegan creamy mushroom pasta + plant cream (2-burner, Oatly/Vitasoy), a35 Vegan schnitzel + mash + mushroom gravy (3-burner, Sunday-Roast-Niveau). Marken-Hints in Generic-Naming („e.g. v2food, Linda McCartney") für Brand-Neutralität. **Generator-Anpassung — Plant-Negation:** neuer `PLANT_BASED_RX = /\b(vegan|plant-based|plant)\b/i`. `containsFreshMeat()` und `meatShelfLife()` ignorieren Zutaten die diesem Marker matchen — sonst würden „Vegan sausages" über `FRESH_MEAT_RX.sausages?` als Frischfleisch erkannt und einen Cluster-Slot blockieren. Plant-Convenience bleibt im **nonMeat-Pool** (cooling:'high' bringt sie früh in Cooling-Sortierung). **CATEGORIES erweitert:** `🥩 Fresh meat & plant proteins`-Regex um `schnitzel|cutlets?|mince|patty|patties|nuggets?` — Plant-Schnitzel/-Mince landen in der Frischfleisch-Einkaufssektion (gleiche Kühlung). Plant-Cream/Vegan-Cheese matchen schon bestehende Dairy-Regex. **Tests +4 in `generator.test.js`:** Vegan-Pool enthält alle 7 IDs (mit `burners:3` für a35), Vegan-Plan über 28 Tage zeigt mind. 1 Convenience-Rezept, Vegan-Convenience NICHT im Frischfleisch-Cluster (small fridge + 7d → alle Slots besetzt), Plant-mince landet in 🥩-Einkaufskategorie via override-Pin. **191 Tests grün** (vorher 187). Bewusst nicht: kein Pricing-Update — Pauschale `🥩 Fresh meat & plant proteins`-Kat ($12/Item) liegt innerhalb der ±30%-Schätzgenauigkeit für Vegan-Schnitzel/Würste; Cooling 'medium' nur für a34 wegen UHT-Plant-Cream-Tetrapack; kein Hardlock auf eine Marke.)

**Letztes Update vorher:** 2026-05-15 (r) (Famous Cape-York-Tracks + Connectivity-Filter. User: „ergänze tracks wie der frenchmans track oder der der zum usshers point führt oder den creb track mit einem hohen detail grad … und achte bei den restilichen, dass genug detail vorhanden ist dass sie nicht im nichts starten oder aufhören, sondern dass immer ein klarer start und endpunkt ersichtlich ist". **Famous Tracks:** ROAD_NAME_MAP +10 neue Patterns (creb, bloomfield, usshers, captainBilly, twinFalls, eliotFalls, vrilya, mapoon, telegraph, capeMelville). **Ergebnis: 11/27 named Strassen extrahiert** — neu CREB Track (96 simplified pts), Bloomfield Track (115 pts), Frenchmans Track war schon (53 pts), Lakefield Rd (157 pts). 8 famous-track Namen sind in OSM nicht so getaggt wie gesucht (Usshers/Captain Billy/Twin Falls/Eliot Falls/Vrilya/Mapoon/Telegraph/Cape Melville sind wahrscheinlich unnamed `highway=track`) — die kommen aber via Connectivity-Filter als generische Tracks durch. **Connectivity-Filter (User-Kern-Wunsch „klarer Start- und Endpunkt"):** generische Tracks werden nur behalten wenn mindestens ein Endpunkt innerhalb 0.005° (~555m) eines Anchor-Punkts liegt. Anchor-Quellen: alle Punkte aller named ROADS + MAJOR_ROADS + RIVERS + POI-Coords aus cape-york-pois.js (dynamisch via `await import()`). Spatial Bucket-Grid mit Bucket-Size = SNAP_DEG → O(1)-Lookup statt O(N²). Pro Endpunkt 9 Buckets (3×3) gecheckt. **OSM-Query erweitert:** `way["highway"="track"]` jetzt OHNE tracktype/name-Vorfilter — alle Tracks runter, Filter entscheidet danach. **Numbers:** Raw 870→**6439 Track-Ways**, Pre-Connectivity 5162 chains, **Post-Connectivity 698 Tracks behalten** (1975 dropped — 74% drop-rate). Vorher (mit min-length-filter) waren es nur 186-241. **Renderer:** neue Konstante `FAMOUS_TRACK_IDS = ['frenchmans','creb','bloomfield','oldTele','oldTeleBypass']`, eigener Render-Loop mit dashed track-Style; `rinyirru` (Lakefield Rd) als gesealter Park-Zugang gerendert; ROAD_LABELS um Frenchmans/CREB/Bloomfield/Lakefield/Portland Roads erweitert. **187 Tests grün**, Bundle JS 1126.83→**1205.86 kB** (+79 kB · gzip ~342→~365 kB), Geo-File on-disk 911→**978 kB**.)

**Letztes Update vorher:** 2026-05-15 (q) (Dismissible Info-Banner — User-Wunsch, Banner sollen wegklickbar sein wenn man sie schon kennt. **Zwei Banner mit ✕ oben rechts:** (1) **MenuTab `ClusterInfoBanner`** („🥩 Fresh meat scheduled on X of Y days …") — Persistenz via `localStorage['ui_dismissed_meatcluster']`. ✕-Button als HTML-Sibling vom Toggle-Button (nested `<button>` ist invalid HTML), absolut positioniert mit `e.stopPropagation()`. Container bekommt `position: relative`, head bekommt `padding-right: 44px` damit ▾-Arrow nicht hinter ✕ verschwindet. (2) **ShoppingTab Note** („🏪 Last big supermarket! …" / „🌿 Small store in Bamaga …") — pro Bucket persistent unter `ui_dismissed_note_<spId>` (Cairns/Bamaga unabhängig dismissable). Neue Sub-Komponente `DismissibleNote({ supplyPoint })` ersetzt drei Code-Duplikate (locked-Mode, Empty-State, Hauptliste); `isStart` aus der Hauptkomponente entfernt. **Storage-Konvention** (aus (m) übernommen): `ui_`-Prefix wird vom resetAllShoppingState-Wipe NICHT erfasst — UI-Pref soll Reset überleben. **CSS:** `.note` wird flexbox (`display: flex; align-items: flex-start; gap: 8px`); neue `.note-text` (flex: 1) und `.note-close` (32x32 Touch, negative margin um padding zu durchbrechen, opacity .55→1 on active). `.info-dismiss` für ClusterInfoBanner: absolute oben rechts, gleicher Look in `var(--tx2)`-Farbe. `.note` wird nur in DismissibleNote genutzt (verifiziert per grep) — flex-change bricht keine andere UI. **Strings:** `S.shopping.dismissAria` und `S.menuJump.dismissAria`. **187 Tests grün**, Build sauber. Bewusst kein „Show all dismissed banners again"-Reset im UI — User würde bei Bedarf den LocalStorage manuell leeren; für 3 Banner wäre Reset-UI overengineered.)

**Letztes Update vorher:** 2026-05-15 (p) (Drastische OSM-Daten-Erweiterung. User: „erweitere deine datenbank von national parks strassen und tracks und wäldern drastisch, ziehe dabei alle nötigen daten von open street map". **Übersicht:** NPs 4→**141** (alle benannten in bbox, Marine ausgefiltert via NP_MAX_BBOX 3.0° + Marine-Regex), Named Roads 7→9 (Frenchmans + Lakefield Road neu gematcht), **Major Roads 0→210 unnamed Segmente** (highway=primary/secondary/tertiary/trunk/motorway, BEWUSST OHNE unclassified — wären tausende Cairns-Stadt-Strassen), **Tracks 0→186 4WD-Track-Segmente** (highway=track mit name oder tracktype), Forests 60→**200** (FOREST_MIN_BBOX 0.08°→0.04°, TOP_N 60→200). **Fetch-Skript:** Overpass-Query timeout 240→540s (heavy query timeout-te vorher mit 504). `extractNationalParks(osm)` umgeschrieben — statt 4 hardcoded IDs nimmt jetzt ALLE benannten boundary=national_park|protected_area|leisure=nature_reserve, bekannte IDs behalten semantische Slugs (np-lakefield etc.) für Label-Aliasse, Rest bekommt `np-${slugify(name)}`. NP-Filter: NP_MIN_BBOX 0.04° (gegen Mikro-Reserves), NP_MAX_BBOX 3.0° (eliminiert Great Barrier Reef Marine Park mit 17.957° bbox-diag), NP_MARINE_RX (eliminiert Marine Areas die über Wasser-Layer rendern würden). Neue Extractors `extractMajorRoads()` + `extractTracks()` via gemeinsamem Helper `extractWaysAsChains()`, coarser DP (0.001° statt 0.0004°) und höhere min-chain-length (0.015°/0.012°) — diese Layer sind Hintergrund-Detail. Dedupe gegen ROAD_NAME_MAP (kein Doppel-Render). **Renderer (`CapeYorkMap.jsx`):** Pre-computed `MAJOR_ROAD_PATHS` + `TRACK_PATHS`. Neue Render-Reihenfolge: Tracks (0.5px dashed `1.5 1.5` opacity 0.55) → Major Roads (0.8px solid opacity 0.7) → Rivers → Trip-Overlay → Named Roads. NP-Render unverändert (Loop iteriert jetzt 141 statt 4 Polygone). **NP_LABELS gecapped auf Top-30** nach Polygon-bbox-Diagonale — bei 141 NPs würden Labels gnadenlos überlappen. Kurz-Namen-Mapping erweitert für Nature Refuge→NR, Indigenous Protected Area→IPA, Forest Reserve→FR. Neuer Alias Kalkajaka→Black Mountain NP. **187 Tests grün**, Bundle JS 589.09→**1126.83 kB** (+538 kB · gzip 181→**342 kB**), Geo-File on-disk 375→**911 kB**. Bundle-Wachstum ist Preis für die drastische Erweiterung — User hatte explizit „drastisch" verlangt und akzeptiert grössere Bundle-Sizes. PWA mit offline-cache: 342 kB gzip beim ersten Load, danach gratis aus Service Worker. Bewusst weggelassen: unclassified highways (Cairns-Stadt-Noise), highway=path|footway (Wanderwege), Marine Protected Areas, per-NP-Layer-Toggle.)

**Letztes Update vorher:** 2026-05-15 (o-fix) (Zwei Bugs aus (o) gefixt nach User-Feedback. **Bug 1:** Sticky-Bar verdeckte ~⅘ der gesprungenen DayCard — `scrollIntoView({block:'start'})` ignoriert sticky overlays. **Fix:** `scroll-margin-top: 64px` auf `.day-card` (Bar ist ~55px + Atemluft). Reines CSS, vom Browser nativ respektiert. **Bug 2:** Card blieb collapsed beim Jump — User musste nach dem Sprung nochmal tappen um Mahlzeiten zu sehen. **Fix:** Open-State von DayCard nach MenuTab geliftet als `Set<number>` (`openDays`); DayCard ist jetzt stateless für open/close. `handleJumpTo(d)` macht erst Aufklappen, dann via `requestAnimationFrame` scrollen (sonst scrollt der Browser zur kollabierten Position und springt beim Expand nach). Today-Initial-Open kommt jetzt aus dem initial Set, nicht mehr aus dem `isToday`-Prop von DayCard. Multi-Open bleibt erlaubt — Accordion-Pattern wäre invasiver. **187 Tests grün**, Build sauber.)

**Letztes Update vorher:** 2026-05-15 (o) (MenuTab-Polish nach ShoppingTab-Polish (m). Auf 16-Tage-Trips ist Navigation durch DayCards mühsam. **Drei Änderungen in `MenuTab.jsx`:** (1) **Neue `DayJumpBar`-Sub-Komponente** — sticky am Top des Tages-Bereichs (`position: sticky; top: 0; z-index: 5`), horizontal scrollbar mit Scrollbar-hide für sauberere Optik. Ein Pill pro Tag, Tap scrollt smooth zur DayCard via `document.querySelector('[data-day="N"]').scrollIntoView`. Bamaga-Tag bekommt grünen 4px-`::after`-Dot, Today-Tag ist orange-filled. Bar zentriert beim ersten Mount den Today-Button via `inline: 'center'`. (2) **Today-Erkennung & DayCard-Marker** — neue Helper-Funktion `currentTripDay(startDateIso, totalDays)` (8 Zeilen, in MenuTab inline; nutzt `parseISO`/`diffDays` aus `lib/dates.js`): liefert 1-basierte Tag-Nummer wenn Trip aktuell läuft, sonst null. Today-DayCard bekommt `.day-today`-Klasse (orange 2px-Border + soft glow box-shadow), kleines orange „TODAY"-Pill neben dem Day-Title, **initial expanded** (User landet im Tab → sieht direkt heutige Mahlzeiten ohne Tap). (3) **Auto-Scroll on Mount** — beim ersten Render des MenuTab springt der Viewport instant (`behavior: 'auto'`) zum heutigen Tag. useEffect mit leerer deps array, dokumentiert via eslint-disable-Kommentar. **Datenfluss:** `config.startDate` ist schon im merged config (App.jsx: `{ ...config, ...result.config }`) — kein Prop-Add nötig. **Strings:** neuer `S.menuJump`-Bucket (`todayPill`, `jumpAria`). Beim Edit kurz eine Klammer-Falle in strings.js (shopping zu früh geschlossen) — sofort gefixt. **CSS-Neu:** `.day-card.day-today`, `.day-today-pill`, `.day-jump-bar`, `.day-jump-btn` (`+.bamaga` mit ::after-Dot, `+.today` orange-filled). **187 Tests weiterhin grün** (keine neuen — currentTripDay ist trivial, könnte später in `lib/dates.js` gehoben + getestet werden). Bewusst nicht: Lib-Hoist von currentTripDay, Mitternachts-Update, Scroll-Spy für Active-Day. Today-Logik ist heute (2026-05-15) noch nicht live testbar — Eigen-Trip startet 2026-06-14; vor dem Trip sieht der User nur die Jump-Bar ohne Today-Highlight.)

**Letztes Update vorher:** 2026-05-15 (n) (Map-UX-Triple — Beschriftungen + Trip-Overlay + Pinch-Zoom/Pan in einem Pass. User: „setzte alle drei verbesserungen durch ohne rückfragen und mit grösster sorgfalt ohne fehler und bugs". **(1) Beschriftungen:** Pre-computed Labels für Rivers (italic, mid-blue, am Mittelpunkt der längsten Chain via `midpointAlongLongest()`), Roads (uppercase, dunkles Braun, 5 Hauptstrassen mit Display-Namen wie „Mulligan Hwy" / „Peninsula Dev Rd" / „Bamaga Rd" / „Old Telegraph Track" / „Pajinka Rd") und NPs (Centroid via Shoelace-Formel, Kurzname-Aliasse Rinyirru→Lakefield NP / Kutini-Payamu→Iron Range NP / Oyala Thumotang→Mungkan NP — nur sichtbar wenn `park`-Layer aktiv). Alle Labels mit weißem Halo via SVG `paint-order: stroke; stroke: var(--map-water/-land)` für Lesbarkeit über variablen Backgrounds. Anker-Labels werden ausgeblendet wenn am gleichen Ort ein Trip-Stop-Marker rendert (kein Doppellabel). **(2) Trip-Overlay:** `App.jsx` reicht `config` als `tripConfig` durch InfoMapTab in CapeYorkMap. `computeTripStops(config)` derive-t aus `enabledStops`/`bamagaStop`/`bamagaDay`/`stopDays` die geplante Stop-Liste, Coords aus CAPE_YORK_POIS via `rs-${id}`-Match. **Route-Highlight:** Mulligan + PDR (+ NPA + Pajinka wenn Bamaga-Stop) in Trip-Orange (`--map-trip-route` neu = `#C0600C`, App-Brand-Color) UNTER den braunen Strassen, breiter Stroke (4.5px) + 45% Opacity → schimmert wie ein Marker auf einer echten Karte. **Trip-Stop-Marker** größer als Resupply-Marker (r=6.5 + r=9 outer halo), Day-Nummer als 8.5px-weiß-bold im Zentrum, Stop-Name (Halo) darunter. `tripStopIds`-Set filtert doppelte Resupply-POIs. **(3) Pinch-Zoom + Pan via viewBox-Manipulation:** State `view = {x,y,w,h}` direkt als SVG-`viewBox` gerendert — Marker/Strokes/Text skalieren mit (real-map-Verhalten, keine Counter-Scaling-Logik nötig). **Pointer Events** (unified Maus/Touch): 1 Pointer = Pan (delta in viewBox-Einheiten via `view.w/element.width`), 2 Pointer = Pinch (scaleFactor = `newDist/prevDist`), Mausrad = Zoom um Maus-Position via *nativem* non-passive Wheel-Listener in `useEffect` (React-Synthetic-Wheel ist seit v17 passive, `preventDefault` würde dort nicht greifen). `zoomAroundPoint(scaleFactor, anchorX, anchorY)` als gemeinsame Zoom-Funktion. `clampView()` clampt `w` zwischen `MIN_W = VIEWBOX_W/6` (max 6×) und `MAX_W = VIEWBOX_W` (min 1×, full map), `h` proportional, `x,y` so dass die viewBox nie außerhalb des Originals liegt. **Marker-Schutz:** `onPointerDown` returnt früh wenn `e.target.closest('.map-marker')` matcht — Marker-Click feuert dann normal, kein Pan-Konflikt. CSS `touch-action: none` auf der SVG (Browser-Default deaktiviert), `setPointerCapture` damit Move/Up auch außerhalb der SVG ankommen. **Zoom-Controls UI:** ＋/−/⟲-Stack rechts oben, 36×36px, halb-transparent weiß, mit `touch-action: manipulation` für sofortige Tap-Response. „Mainland continues south"-Footer wird bei Zoom > 1× ausgeblendet. **187 Tests grün**, Bundle 578.71→**589.09 kB JS** (+10 kB · gzip 176→181 · CSS 36.55→38.18 kB). Bewusst nicht: Counter-Scaling, Doppeltap-Zoom, GPS „center on me" — ersteres unintuitiv für Map-UX, zweiteres kollidiert mit Browser-Text-Selection, letzteres braucht Permissions und gehört in Stufe 2.)

**Letztes Update vorher:** 2026-05-15 (m) (ShoppingTab-Polish-Iteration nach der Cost-Schätzung. **Drei Änderungen:** (1) **Sticky `progress-card`** — `position: sticky; top: 0; z-index: 5`. Counter, Cost und Hide-Toggle bleiben sichtbar während der User durch lange Listen scrollt. Reines CSS. (2) **Stärkeres Dimming für abgehakte Items** — neue Klasse `.chk-item.done` mit opacity .55 + leicht abgesetztem Background `#FAF8F5`, `.chk-edit` zusätzlich auf .6. CheckItem setzt die `done`-Klasse jetzt direkt auf `chk-item` (vorher nur auf chk-name/chk-qty). Smooth `.2s`-Transition. (3) **„👁 Hide X checked"-Toggle** zwischen Cost-Sektion und Reset/Check-all-Buttons. Pro Bucket persistent unter `ui_hide_checked_<spId>` in localStorage. Filter-Logik: items mit `checked[id]=true` werden ausgeblendet, leere Kategorien gedropt (kein nackter Cat-Header). Toggle nur sichtbar wenn `checkedCount > 0`, Label switcht Hide ↔ Show mit live-Count. **Storage-Konvention `ui_`** neu eingeführt für UI-Präferenzen — wird vom `resetAllShoppingState()`-Wipe NICHT erfasst (Pref ist Komfort, nicht Trip-State). Strings: `S.shopping.hideChecked` / `showChecked` mit `count`-Parameter. CSS: `.progress-hide*`, `.chk-item.done`, transition auf `.chk-item`. **187 Tests weiterhin grün** (keine neuen Tests — UI-only-Änderung). Build sauber, Bundle marginal (+~0.4 kB JS, +~0.4 kB CSS). Bewusst nicht: kein Reorder beim Toggle (User würde Items verlieren), kein Auto-Hide bei 100%, kein Sticky-Offset für Topbar.)

**Letztes Update vorher:** 2026-05-08 (l) (Grobe Kostenschätzung in der Einkaufsliste. User: „bei der einkaufslist wäre eine ungefähre schätzung der einkaufskosten noch interessant". Nach AskUserQuestion: Pauschalpreise pro Kategorie, Anzeige oben in der progress-card. **Neue Datei `src/lib/pricing.js`** mit drei Faktoren: (1) Pauschalpreis pro Item-Kategorie in AUD (Cairns-Baseline — Frischfleisch $12, Pasta/Rice $3.50, Frischgemüse $3, Spices $4, Camping-Essentials $7, Fallback $4); (2) qty-Multiplier parst kg/g/ml/L (Baseline 500g = 1.0x), count-Einheiten linear (packs/cans/bottles/jars/tins/loaves/rolls/boxes/bags/sachets), Cap bei 6 für reine Zahlen, Range → Mittelwert, „as needed" → 1.0; (3) Supply-Point-Multiplier (Cairns 1.0x, Cooktown 1.3x, Coen 1.6x, Archer 1.7x, **Bamaga 1.8x**). API: `estimateBucketCost(data, spId)`, `estimateRemainingCost(data, checked, spId)`, `formatAud()`. **`ShoppingTab.jsx`** progress-card erweitert: zwischen progress-bar und actions neue cost-Sektion mit „💰 ~$340 AUD" links + „~$220 left" rechts (nur wenn 0 < checked < total) und italic-muted „Rough estimate · prices vary by store and season". `useMemo` re-berechnet bei jedem checkbox-toggle. Strings in `S.shopping.cost`. CSS: `.progress-cost*`-Klassen, orange-Akzent für „X left", 11px italic-Disclaimer. **187 Tests grün** (vorher 173, +14 für pricing.test.js — qtyMultiplier mass/volume/count/range/empty, estimateBucketCost mit Cairns- + Bamaga-Multiplier + Fallback, estimateRemainingCost mit checked-Filter, formatAud rounding + invalid input). Architektur folgt CLAUDE.md § 1: Stufe 2 kann eine echte Preisliste pro Zutat anbringen, ohne dass die UI angefasst werden muss. Erwartete Genauigkeit ±30–40 % — reicht für UX-Zweck „was kostet mich das ungefähr?".)

**Letztes Update vorher:** 2026-05-08 (k-fix7) (Fünf-Iterationen-Push gegen „endet im Nichts"-Effekt. User: „schon 1000x besser, aber es gibt immer noch flüsse tracks und strassen, die im nichts beginnen oder enden, führe fünf iterationen durch wo du in jeder die präzision und vollständigkeit der vorherigen verbesserst". **Diagnose:** `chainWays()` lieferte nur die LÄNGSTE Chain pro Feature, Tributaries/Branches verworfen; Chain-Tolerance 0.0001° (~10m) zu eng; viele Cape-York-Rivers als `waterway=stream` getaggt; strenge `/^X river$/i`-Patterns verfehlten Creek-Varianten; kurze Rest-Segmente (2-3 pts) blieben als Stubs übrig. **Iter 1**: `chainWaysAll()` für Rivers — Datenmodell `{segments:[[]]}` statt `{points:[]}`, alle Chains gerendert (Normanby 1→9 chains). **Iter 2**: Same für Roads (`{[id]:{segments:[[]]}}` statt `{[id]:[]}`), Chain-Tolerance 0.0001°→**0.0003°** (mulligan 1→79 chains, pdr 1→11). **Iter 3**: `waterway=stream` zur Overpass-Query, RIVER_NAME_MAP +11 Old-Tele-Creeks (Lukin neu gematcht; Nolan Creek + Bertie Creek + Cholmondeley Creek neu). **Iter 4**: ROAD_NAME_MAP +10 Cape-York-Tracks (Frenchmans/Heathlands/Seven Mile/Wakooka/Starcke/Rinyirru/Battle Camp/Kalpowar/Cooktown-Dev) + aufgeweichte Patterns (Old Telegraph Road/Track-Variante, Iron Range Road, Southern Bypass) — keine neuen Matches aber Robustheit. **Iter 5**: Alle River-Patterns von `/^X river$/i` auf `/^X (river|creek)$/i` (Stewart neu gematcht, Archer zusätzlich als Archer Creek 1→2 chains, Kennedy Creek 102→**229 pts** mit 5 chains!) + Post-DP-Stub-Filter (Rivers <4 pts und Roads <3 pts werden verworfen — eliminiert visuelle Stub-Stücke „im Nichts"). **Endergebnis:** Rivers 13→**17 gematcht**, mit Multi-Segment-Rendering. Roads 7/7 mit Multi-Segment (mulligan 280→639 pts, npaRoad 257→344, oldTele 68→115). NPs + Land-Polygon + Forest unverändert (k-fix6). **CapeYorkMap.jsx:** RIVERS-Render verwendet `flatMap` über segments; neuer `renderRoadSegments()`-Helper rendert alle Road-Segmente mit konsistenter Stroke-Config. **173 Tests grün**, Bundle 556.93→**578.71 kB JS** (+22 kB · +5 kB gzip → 176 kB gzip). Was NICHT fixbar war: 6 Rivers (Olive/Coen/Edward/Watson/Lockhart) wirklich nicht in OSM; 8 Old-Tele-Creeks nicht oder als 2-pt-Stubs. Diese sind keine Pipeline-Probleme sondern OSM-Daten-Lücken — sie werden gar nicht erst gerendert, also kein „endet im Nichts" mehr durch sie.)

**Letztes Update vorher:** 2026-05-08 (k-fix6) (Skalen-Konsistenz auf der Map. User: „I do not need more complexity and layors, but more detail, it is important that the road is shown correct, that they are on the right position on the map for example, everything needs more detail not yust the roads, but the rivers, national parks and forest areas, and make sure that they fit together scale whise". **Diagnose:** Land-Outline war hand-skizziert (41 Wegpunkte), während Roads/Rivers/NPs/Coastline aus OSM kamen → Roads lagen in absolut korrekten Positionen, sahen aber „neben dem Land" aus. Forests waren 3 hand-platzierte Ovale ohne Realdaten-Bezug. **Fix:** (1) **`extractLandPolygon`** in `scripts/fetch-osm-geo.mjs` — chained OSM-Coast-Ways zur Mainland-Linie, DP-vereinfacht ε=0.0008° (~88m), Süd-Schluss bei lat=-19.10° für Mainland-continues-Effekt. **4702 Wegpunkte**. (2) **`extractForests`** — `natural=wood` + `landuse=forest` (Ways + Relations), filtert auf bbox-diag ≥ 0.08° (~9 km), DP ε=0.003° (~330m), sortiert nach Größe, **Top-60** im Bundle (260 Kandidaten gefunden). 2188 Punkte total. (3) **`CapeYorkMap.jsx`** — 41 hand-Outline-Punkte raus, OUTLINE_PATH aus LAND_POLYGON gebaut; 3 VEGETATION_PATCHES-Ovale raus, FOREST_PATHS aus 60 OSM-Polygonen ersetzt. SVG-`<clipPath id="land-clip">` clippt Forests auf Land (kein Bleeding ins Wasser bei OSM-Imprecisions). COASTLINES-Detail-Overlay raus (redundant — Land-Polygon IST jetzt die Coast-Linie). Strand-Stroke 1.8→1.0. (4) **Compact JSON output** im Generator-Skript — geo-File 434→**353 kB** trotz neuer Daten. **173 Tests grün**, Bundle 533.98→**556.93 kB JS** (+23 kB · +6 kB gzip → 171 kB gzip). Skalen-Konsistenz jetzt strukturell garantiert: Land-Polygon, Roads, Rivers, NPs, Forests kommen ALLE aus einem Overpass-Query, durch dieselbe DP-Pipeline → wenn die PDR an Cooktown vorbeiläuft, liegt sie exakt auf dem Land-Polygon, kein „Road schwebt im Wasser"-Effekt mehr möglich.)

**Letztes Update vorher:** 2026-05-05 (k-fix5) (Vier-Iterationen-Push für maximale Geo-Genauigkeit. User: „führe einige iterationen durch um die genauigkeit der park grenzen zu verbessern, und der flüsse und strassen. Du kannst auch mehr details und grössere dateien der open street map dazu beziehen". **Iteration 1**: DP-Tolerances verschärft — Flüsse 0.005°→0.002° (~220m), NPs 0.002°→0.0008° (~88m), Strassen 0.001°→0.0004° (~44m). NPs wachsen von 157→**266 pts** (Lakefield), 41→171 (Iron Range), 89→**488 pts** (Apudthama!), 30→166 (Mungkan). PDR von 267→454 pts, Mulligan 169→280 pts. **Iteration 2**: 14 zusätzliche Flüsse im Pattern — 13/21 gematcht: Stewart/Olive/Coen/Edward/Watson/Lukin/Lockhart/McIvor sind in OSM nicht als `waterway=river` getaggt (vermutlich `stream`). Neue Hits: Dulhunty (63 pts), Hann (15), Kennedy (37), Morehead (14), Holroyd (8), Annan (52). **Iteration 3**: Old Telegraph endlich richtig — Pattern aufgesplittet in `oldTele: /^old telegraph track$/` (die ECHTE 4WD-Track, 4 ways → 68 pts) und neuer `oldTeleBypass: /^old telegraph line bypass$/` (15 pts, dünner gestrichelt rendert). `npaRoad`-Pattern erweitert um Klammer-Suffix `(Old Telegraph Line)`-Variante → 24→48 ways, 42→257 pts (6× detaillierter). **Iteration 4**: Echte OSM-Coastline als Detail-Overlay — `natural=coastline`-Query in der Pipeline, neue `extractCoastline()`-Funktion mit chainWaysAll-Logik, Filter auf signifikante (≥20 pts), DP-vereinfacht. Output: **212 Coast-Polylines mit 5873 simplified Wegpunkten**. CapeYorkMap.jsx rendert sie als feine 0.6-Stroke-Detail-Linie über dem Land-Fill (deeper-water Color, 55% Opacity) — Hand-Outline bleibt für Mainland-Continues-South-Effekt. **173 Tests grün**, Bundle 366.22 → **533.98 kB JS** (+167.7 kB · +35.9 kB gzip · Vite-Warning bei 500-kB-Schwelle, aber für offline-PWA via SW-Cache akzeptabel — erster Load 165 kB gzip, dann offline). Geo-File on-disk 74 → 434 kB. Map zeigt jetzt: echte 5000+ Coast-Wegpunkte, echte Old-Tele-Track separat von Bypass, 488-Punkt-Apudthama-Grenze, 13 Flussverläufe — alles aus OSM. „Iteration vorbei" wenn der User sagt, es reicht.)

**Letztes Update vorher:** 2026-05-04 (k-fix4) (NP-Detail verfeinert + Strassen aus OSM: User-Wunsch „erhöhe die qualität und die detail genauigkeit der umrisse der national parks im gleichen masse und auch der strassen". Dieselbe Pipeline wie k-fix3, aber mit Layer-spezifischer DP-Tolerance: Flüsse 0.005°, **NPs 0.002° (~220m, vorher 0.005°)**, **Strassen 0.001° (~110m)**. Overpass-Query um `way["highway"]["name"~"Mulligan|Peninsula Developmental|Bamaga Road|Old Telegraph|Captain Cook|Pajinka|Lockhart Road|Portland Roads"]` erweitert. **Ergebnis 6/6 Strassen extrahiert:** Mulligan/Captain Cook (454 ways → 169 simplified pts), PDR (136 ways → 267 pts!), Bamaga Road / NPA (24→24), Old Telegraph Bypass (9→54), Pajinka Road (6→40), Portland Roads / Lockhart-Branch (2→19). **NPs deutlich detaillierter:** Lakefield 68→157pts, Iron Range 41→78, Apudthama 89→208, Mungkan 30→65. **Hand-coded `ROADS`-Object aus `CapeYorkMap.jsx` raus**, jetzt Import aus `cape-york-geo.js` (nur Datenmodell, Render-Code unverändert). `pathFromPoints()` toleriert undefined (falls Match fehlt, Pfad einfach unsichtbar — kein Crash). Strassen folgen jetzt geographisch korrekten Verläufen — nicht mehr meine 6-Wegpunkt-Skizzen aus Trainingswissen. Old Telegraph Track matched als „Old Telegraph Line Bypass" (der offizielle Bypass-Weg, nicht die ungesealte 4WD-Track) — kann später feiner aufgespalten werden falls beide separat gerendert werden sollen. **173 Tests grün**, Bundle 348.30 → **366.22 kB JS** (+17.9 kB · +6.4 kB gzip · CSS unverändert). Geo-File 35 → 74 kB on-disk (vor Minification). Map zeigt jetzt: echte QPWS-NP-Grenzen + echte OSM-Strassenverläufe + echte Flussverläufe — komplett OSM-getrieben statt Hand-Daten.)

**Letztes Update vorher:** 2026-05-04 (k-fix3) (OSM-Pipeline ersetzt Hand-Polygone: User-Feedback „es ist immer noch ziemlich ungenau". Strukturelles Problem: Hand-skizzierte Geo-Daten aus Trainingswissen sind zwangsläufig ungenau, weil offizielle QPWS-Polygone tausende Wegpunkte haben, die ich nicht im Kopf habe. **Lösung: build-time OSM-Fetch** analog zur bestehenden Tankstellen-Pipeline. Neues Skript `scripts/fetch-osm-geo.mjs` (Overpass-API → echte OSM-Geometrien für `waterway=river` und `boundary=protected_area|national_park` in Cape-York-BBox), Douglas-Peucker-Vereinfachung mit ε=0.005° (~550m), Output `src/data/cape-york-geo.js` als Auto-Generated-File. Re-Refresh via `npm run geo:refresh`. **Ergebnis 7/7 Flüsse + 4/5 NPs:** Jardine (5 ways→1686pts→52 simplified), Wenlock (3→1338→117), Pascoe (1→724→28), Archer (1→65→7), Normanby (3→587→55), Endeavour (4→280→14), Mitchell (13→1737→149); Lakefield/Rinyirru (1273→68), Iron Range/Kutini-Payamu (1026→41), Apudthama (4222→89), Mungkan/Oyala Thumotang (1346→30). **`np-jardine` keine Match** — das alte Jardine River NP wurde 2014 in Apudthama integriert, ist offiziell kein eigenständiger Park mehr. Polygon entfällt damit, der `np-jardine`-POI bleibt als Marker erhalten. **`cape-york-pois.js` re-exportiert** RIVERS + NP_POLYGONS aus dem neuen Geo-File — `CapeYorkMap.jsx` braucht keine Code-Änderung, gleiches Datenmodell. **173 Tests grün**, Bundle 335.12 → **348.30 kB JS** (+13.2 kB · +5.8 kB gzip). Höhere Bundle-Kosten sind der Preis für echte Geometrie: 4 NP-Polygone mit 228 Wegpunkten + 7 Flüsse mit 422 Wegpunkten = 650 echte OSM-Wegpunkte statt ~50 hand-skizzierte. Bedeutet: Map zeigt jetzt geographisch korrekte Park-Grenzen + realistische Flussverläufe. ODbL-Attribution bleibt im AboutTab.)

**Letztes Update vorher:** 2026-05-04 (k-fix2) (NP-Polygone detaillierter + 7 Hauptflüsse als eigene Layer-Schicht: User-Feedback „versuche die formen der national parks noch genauer ein zu zeichnen, dass sie mehr der realität entsprechen, und mit einem anderen blau zeichnest du noch die verläufe der wichtigsten flüsse ein". (1) Alle 5 NP-Polygone von 4 auf 6–10 Wegpunkte erweitert (Lakefield 10pt mit West-Lappen + Princess-Charlotte-Bay-Erweiterung; Iron Range 7pt entlang der Lockhart-River-Region; Jardine 7pt entlang dem Jardine-Flusslauf; Apudthama 7pt mit Multi-Lappen-Form für Tip-Region; Mungkan/Oyala Thumotang 7pt westlich Coen). (2) **Neue `RIVERS`-Konstante** in `cape-york-pois.js` mit 7 Cape-York-Hauptflüssen: Jardine (5pt, Heathlands→Ferry→Karpentariengolf), Wenlock (4pt), Pascoe (3pt, Iron-Range-Region), Archer (4pt, durch Archer River Roadhouse), Normanby (4pt, Lakefield-Drainage), Endeavour (3pt, Cooktown-Mündung), Mitchell (4pt, südlich — macht klar dass Mainland weitergeht). (3) **Neue CSS-Variable `--map-river: #4A7FB8`** — mid-blue, klar verschieden vom türkis-grünen Salzwasser. (4) Render-Reihenfolge in `CapeYorkMap.jsx`: NP-Polygone → **Flüsse** → Strassen — d.h. Strassen liegen optisch über den Flüssen (wie Brücken). Flüsse werden immer angezeigt (nicht layer-toggle-gated, da Wasserläufe Pflicht-Geographie sind). 173 Tests grün, Bundle 332.50 → 335.12 kB JS (+2.6 kB · +0.3 kB gzip — 7 Flüsse + erweiterte NP-Polygone als reine Datenpunkte, sehr günstig im Bundle).)

**Letztes Update vorher:** 2026-05-04 (k-fix) (Map-Verbesserungen v2 nach Live-Feedback: 6 konkrete Bugs/Ungenauigkeiten gefixt. (1) Tankstellen-Positionen: nutzen jetzt echte OSM-`lat`/`lon`-Felder statt linearer Cairns→Bamaga-Projektion (waren teils im Wasser). (2) Vegetation: Einzelnes Oval entfernt (saß im Wasser), durch 3 Land-Patches an strategischen Positionen ersetzt (Iron Range, Lakefield, Central). (3) Old Telegraph Track: 14 statt 6 Wegpunkte mit echten Crossing-Stops (Palm Creek, Ducie, Cypress, Bertie, Cholmondeley, Cannibal Creek, Sailor, Indian Head, Mistake, Nolan's Brook). (4) Marker-Tap-Bug: SVG-`pointer-events`-Issue — `transparent` fill ist nicht klickbar bei `visiblePainted`. Fix: 14px-Hit-Area-Circle mit `fill="white" fillOpacity="0.001" pointerEvents="all"` → 28px Touch-Diameter, daumen-tauglich. (5) NP-Polygone: 5 vereinfachte Polygone (Lakefield/Iron Range/Jardine/Apudthama/Mungkan Kandju) als gestricheltes Grün — werden gerendert wenn 'park'-Layer aktiv. (6) Cape York wirkte wie Insel → MAP_BOUNDS südlich erweitert auf -19°, Outline geht über viewBox-Bottom hinaus, Footer-Text „↓ Queensland mainland continues south ↓" macht klar dass es weitergeht. 173 Tests grün, Bundle ~332 kB JS unverändert.)

> Diese Datei ist der **Einstiegspunkt für jede neue Claude-Session.**
> Sie beantwortet: „Wo stehen wir gerade, was geht, was fehlt, was ist als Nächstes sinnvoll?"
> Bei jeder Änderung am Projekt → diese Datei aktualisieren (siehe `CLAUDE.md` § Workflow).
> Produkt-Brief (Zielnutzer, Kern-Problem, Top-5-Features, Nicht-Ziele, Erfolgsmetrik) → `PRODUCT.md`.

---

## Aktueller Stand (TL;DR)

**Cape-York-Map-Tab + Daysheet-Fuel-Section ersetzt (2026-05-04 k):** Großer UX-Pivot. **Anlass:** Der User hat zurecht angemerkt, dass die Daysheet-„Estimated position: ~500km from Cairns"-Schätzung fragwürdig war — lineare Interpolation Cairns→Bamaga nach Trip-Tag, ohne dass die App weiß wo der User wirklich ist (Pausen, Side-Trips, etc.). **Lösung:** Statt zu schätzen, eine richtige interaktive Karte. **Neue Datei `src/data/cape-york-pois.js`** mit 47 kuratierten POIs in 8 Layern (Resupply 5, Beaches 7, River crossings 6, Campgrounds 8, Waterfalls 5, National Parks 5, Historical 6) + LAYERS-Konstanten + MAP_BOUNDS für SVG-Projektion. **Neue Datei `src/components/CapeYorkMap.jsx`** mit handgezeichneter SVG-Halbinsel-Outline aus geographischen Wegpunkten (Cairns → Cape Trib → Cooktown → Cape Melville → ... → Tip → Vrilya → Mapoon → Aurukun → Diagonale zurück), Strassen-Pfaden (Mulligan/PDR/NPA solid, Old Telegraph gestrichelt), 6 immer-sichtbaren Anker-Labels (Cairns/Cooktown/Coen/Bamaga/Tip/Weipa), und Layer-spezifischen Markern. Linear-Projektion lat/lng → 400×600 viewBox. **Neue Datei `src/components/InfoMapTab.jsx`** mit Map oben, 8 Layer-Toggle-Pills (2-Spalten-Grid) unten, Bottom-Sheet bei Marker-Tap. **Premium-Gating:** Resupply/Fuel/Beaches/Crossings sind free (Pflicht-Wissen + Sicherheit); Campgrounds/Waterfalls/Parks/Historical sind premium-gated — Toggle-Pills bleiben sichtbar mit 🔒-Marker, Tap öffnet PremiumInfoTab (= Marketing-Sale). **Farbschema (vom User vorgegeben):** Wasser türkis-grün `#6FBFB0` (Korallensee + Karpentariengolf), Land warmes Beige `#EFE6CE` (Strand-Akzent), Vegetation frisches Grün `#7AAE85` (Halbinsel-Innere), Strassen warmes Braun `#6B4E2D`. CSS-Variablen in `index.css` zentralisiert. **Tab-Integration:** 🗺️ Map als 3. Tab in Bottom-Nav (zwischen Recipes und Shopping-Stops) **und** als 🗺️-Icon im Home-Topbar (zugänglich ohne Trip-Konfiguration → ideal für Pitch + Werbevideo). **Daysheet-Fuel-Section komplett raus** (24 Zeilen JSX entfernt + estimateRoutePosition-Import). `route-position.js` und `S.config.daySheet.fuel*` bleiben (nicht mehr genutzt, aber non-breaking) — Cleanup ist Stufe-2. **173 Tests grün** (+8 für POI-Datenstruktur: ID-Uniqueness, MAP_BOUNDS-Containment, Layer-Konsistenz, premium-Flag-Konsistenz). Bundle 317.38 → 332.50 kB JS (+15 kB · +5.4 kB gzip · +2 kB CSS). Größter Wachstumsfaktor: 47 POI-Records mit Blurbs + SVG-Wegpunkte + InfoMapTab/CapeYorkMap-Komponenten.

**Konservierte Fleisch/Fisch + transparente Cluster-Logik (2026-05-04 j):** Drei zusammenhängende Lücken adressiert. **(1) Pool für Off-Cluster-Tage erweitert:** 5 neue shelf-stable Omnivore-Rezepte (m22 Spam fried rice, m23 Canned salmon rice bowl, a30 Smoky tuna and white bean stew, a31 Bully beef hash with crispy eggs, a32 Sardine spaghetti). Diese erscheinen automatisch jenseits des Frischfleisch-Clusters, weil der bestehende `containsFreshMeat()`-Filter sie via `SHELF_STABLE_RX` (canned/in-oil/in-brine) als nonMeat klassifiziert. Pool 63 → **68 Rezepte** (13f / 23m / 32a · 27 omnivore / 16 vegetarian / 23 vegan · 45 1-burner / 17 2-burner / 6 3-burner). **(2) SwapSheet filtert konsistent:** an Off-Cluster-Tagen werden Frischfleisch-Optionen ausgeblendet. Generator exportiert neu `config.meatAllowedDays` als sortiertes Array<number> (Cluster-Tage von Cairns + Bamaga). SwapSheet checkt `meatAllowedDays.includes(dayNum)` — wenn nicht enthalten und Diät === omnivore, läuft `compatibleRecipesForCat({ includeMeat: false })` und ein **erklärender Hint** erscheint oben im Sheet („Fresh meat is hidden for this day — it would have spoiled by now"). **(3) Cluster-Logik im UI dokumentiert:** kollabierbarer **Erklärungs-Banner** im MenuTab (nur omnivore + es gibt Off-Cluster-Tage). Tagline „Fresh meat scheduled on X of Y days — tap for why", aufgeklappt: Erklärung der Fridge-Cluster-Mathe + Liste der Substitute (vegetarian/vegan, canned tuna/salmon/sardines/Spam/Bully beef, Eier/Käse/Halloumi) + Bamaga-Reset-Hinweis. Kein Dismiss — User soll es jederzeit nachschlagen können. **165 Tests grün** (+2 für `meatAllowedDays`-Korrektheit: prüft Cairns- + Bamaga-Cluster + Sortierung). Bundle 307.30 → **317.38 kB JS** (+10 kB · +2.8 kB gzip · +1 kB CSS). Diese Iteration schließt die häufigste Customer-Verständnis-Lücke: „Wo ist das Fleisch an späteren Tagen?" — jetzt mit drei Antworten gleichzeitig (mehr Substitut-Rezepte im Pool, Hint im Swap-Flow, Erklärungs-Banner im Plan-Header).

**Vegan-Branding subtiler (2026-05-04 i):** UX-Anliegen vom User: das prominente „Vegan "-Präfix im Rezeptnamen (z.B. „Vegan banana pancakes", „Vegan Buddha bowl…") wirkte für Nicht-Veganer abschreckend, obwohl die App eine breite Zielgruppe adressiert. **11 Rezeptnamen** umbenannt (Banana pancakes, Overnight oats with chia, Tofu scramble breakfast wrap, Buddha bowl with crispy tofu, Chickpea curry with rice, Black bean chili with rice, Mushroom risotto, Tofu stir-fry with noodles, Lentil-vegetable stew, Pasta aglio e olio with chili, Thai green curry feast with crispy tempeh). Stattdessen neue **`VeganBadge`-Komponente** (`src/components/VeganBadge.jsx`) — Inline-SVG, grünes V (`#3F8C2C`) mit kleinem ovalen Blatt (`#6BB144`) am rechten Strich, ~14px, orientiert an Vegan-Society/EU-V-Label-Standard. CSS-Klasse `.vegan-badge` mit `vertical-align: -2px` und 6px-Margin nach links. Verdrahtet in **MenuTab** (Plan-Slot), **RecipesTab** (Card-Header) und **SwapSheet** (Wahl-Cards). **Generator-API leicht erweitert**: `mealEntry()` liefert jetzt zusätzlich `d: recipe.diet` im Plan-Output, sodass MenuTab ohne RECIPES-Lookup darauf reagieren kann. 163 Tests grün (Generator-Tests bestehen, der Plan-Slot ist jetzt nur reichhaltiger). Bundle 306.63 → 307.30 kB JS (+0.7 kB · +0.26 kB gzip · +0.09 kB CSS für die Badge-Klasse).

**3-Burner-Support + Rezept-Pool +12 (2026-05-04 h):** Configurator hat jetzt eine dritte Burner-Pill „3 burners — Big rig / family camper" für große Camper (Britz Frontier, Apollo Trailblazer, Familien-RVs). Generator akzeptiert `burners=3`, `BURNERS = [1, 2, 3]`-Whitelist erweitert, Default bleibt 2 (mittlerer Camper). **6 neue 3-Burner-Rezepte** für „Sunday-Roast-Niveau"-Mahlzeiten die parallele Pots/Pans nutzen: f11 Big Aussie breakfast (bacon+eggs+sausages+beans+mushrooms+toast), m18 Smashburger with chips & slaw, m19 Vegan Buddha bowl mit crispy tofu, a25 Pan-roast chicken with mash & honey carrots, a26 Vegan Thai green curry feast mit crispy tempeh, a27 Mediterranean halloumi feast (vegetarian). **6 weitere Rezepte** für Pool-Diversität — Vegan-1-Burner war kritisch (4f/5m/5a) und ist jetzt aufgestockt: f12 Tofu scramble breakfast wrap, m20 Roasted veggie hummus pita, a28 Coconut dal mit basmati rice. Vegetarian-2-Burner-Boost: f13 Shakshuka with crusty toast, m21 Halloumi & lemon couscous bowl, a29 Brown butter sage gnocchi. **Pool nach Erweiterung: 63 Rezepte** (13f / 21m / 29a · 24 omnivore / 16 vegetarian / 23 vegan · 41 1-burner / 16 2-burner / 6 3-burner). 163 Tests grün (+1 Regression-Test für `burnersApplied=3`); Bundle 288.52 → 306.63 kB JS (+18.1 kB · +5.3 kB gzip — die 12 Rezepte mit voller Zutaten-/Schritt-Liste sind der Wachstumsfaktor). 3-Burner-Rezepte sind als Premium-Verkaufsargument gedacht: nur Big-Camper-Mieter brauchen sie, was wiederum die Premium-Käufer-Zielgruppe ist (höheres Mietwagen-Budget = höhere Zahlungsbereitschaft).

**Shop-Integration v1 (2026-05-04 g):** Premium-Kauf hat jetzt einen aktiven Pitch-Pfad. Neue Datei **`src/lib/checkout.js`** kapselt „wie kauft der User?" — `getCheckout()` returnt je nach Plattform + Config eines von drei Action-Objekten: `{ type: 'native', execute }` (Stufe 2 Play-Billing, Stub), `{ type: 'stripe', url }` (sobald `STRIPE_PAYMENT_LINK` gesetzt ist), `{ type: 'mailto', url }` (Default-Fallback, kein Setup nötig). **Pricing-Konstante zentral: `PRICE = { amount: 15.99, currency: 'AUD', display: 'AUD$15.99' }`** — Charm-Pricing, deckt 7-CHF-Netto-Floor auch nach 15%-Play-Billing-Fee (15.99 × 0.85 = 13.60 AUD ≈ 7.80 CHF). **PremiumInfoTab** umgebaut — primärer 52px-Orange-Button **„Unlock the full plan — AUD$15.99"** (Link bei Stripe/Mailto, Click-Handler bei Native), darunter sekundärer Email-Text-Link (nur wenn primärer Pfad nicht-mailto), darunter Activate-Key-Outline-Button. Pricing-Block: Heading „Unlock the full plan" + 16px-Bold-Preisline „AUD$15.99 · one-time purchase, this device" + Body „No subscription. No account. The license key activates Premium offline …". 162 Tests grün; Bundle 287.05 → 288.52 kB JS (+1.5 kB · +0.4 kB gzip). **Strategie-Pivot zu Play Billing primär** — User priorisiert vertraute Zahl-Umgebung (gespeicherte Karte / Google Pay) über Fee-Optimierung; Stripe-Web bleibt parallel für PWA-Käufer. **Phasen-Modell: A (jetzt) live** — mailto-Fallback, sofort pitch-bar. **B (~30min) wenn User Stripe-Account anlegt** — `STRIPE_PAYMENT_LINK` setzen für PWA-Web-Käufer. **C (~10min) Cloudflare Worker** — deferred. **D (Stufe 2, primärer Pitch-Channel) Capacitor + Play Billing** — nach Eigen-Trip.

**Premium-Sales-UX-Iteration (2026-05-04 e):** Drei Schwachstellen aus dem ersten Premium-Rollout adressiert. **Configurator**: `PillPicker` hat jetzt einen `locked`-Prop — Label und Hint („Fridge size — Cold storage capacity") bleiben lesbar, nur die Pill-Buttons selbst werden geblurrt. Der User versteht so was er als Premium-Feature freischalten würde. **RecipesTab**: pro Kategorie (Frühstück/Lunch/Dinner) werden Free-Rezepte zuerst gerendert, dann ein orange Divider „🔒 X MORE RECIPES WITH PREMIUM", dann die gegate Cards. Premium-User sehen den Divider nicht. **ShoppingTab**: Locked-Tabs zeigen nun eine Sample-Liste mit den ersten 5 echten Items (Name + Menge lesbar) plus einen großen Premium-CTA-Button mit konkretem „Unlock 22 more items + checkboxes, edits, and Bamaga essentials →"-Label statt einer abstrakten Anzahl. Tests bleiben bei 162, Bundle +1.5 kB JS.

**Free/Premium-Tier (2026-05-04 d):** Erstes Monetarisierungs-Konstrukt. Free-Tier zeigt Tag 1–5 vom Plan, nur Cairns-Shopping, Recipes aus den ersten 5 Tagen; Fridge-Size, Compressor-Toggle und Allergien sind gegated. Premium schaltet alles frei. **License-Key-System** (`lib/premium.js`): 16-stelliger Code im Format `XXXX-XXXX-XXXX-XXXX`, 12 Zeichen Payload + 4 Zeichen HMAC-SHA256-Checksum gegen Shared Secret. Verifikation lokal via WebCrypto, kein Server-Call → CLAUDE.md § 5 bleibt intakt. Codes generierbar via `node scripts/generate-license.mjs <email>` oder `--batch N`. **PremiumGate**-Component blurrt Children + zeigt Sticker mit Tap-CTA. Neue Views: `PremiumInfoTab` (Feature-Liste + mailto:<dev-email> + „Already have a key?") und `AccountTab` (Activate-Input + Status + Deactivate). Erreichbar via 👤-Icon im Home-Topbar (Gold-Highlight bei aktivem Premium). Gates an Configurator-Pills (Fridge/Compressor/Allergies), MenuTab (Day-Cards 6+), RecipesTab (Rezepte aus Tag 1–5), ShoppingTab (Bamaga/Cooktown/Coen/Archer). Bottom-Nav zeigt gegate Stop-Tabs mit `🔒`-Prefix. Tests 147 → 162 (+15 für premium-Modul, jsdom als neue devDep für localStorage/WebCrypto in Tests). Kontakt-E-Mail global auf <dev-email> vereinheitlicht.

**Pickup/Dropoff-Logistik (2026-05-04 c):** Tag 1 fällt das Frühstück weg (Vehicle-Pickup nach 10am), letzter Tag fällt das Dinner weg (Drop-off vor 5pm). Generator setzt für diese Slots `{ skip: true, kind: 'pickup'|'dropoff' }`, was weder den Rezept-Pool noch die Einkaufsliste antastet (Restaurant-Slots/Overrides für diese Slots werden ignoriert — semantisch hat der User das Auto noch nicht/nicht mehr). MenuTab rendert die Skip-Slots als kursive graue „🚙 Vehicle pickup …"-Hint-Zeile. Counts-Statistik überspringt Skip-Slots (7-Tages-Trip zeigt 6 Frühstücke + 6 Dinner statt fälschlich 7+7). 1-Tages-Trip = nur Lunch (Tag 1 = letzter Tag, beide Skip-Regeln greifen). Tests 143 → 147 (+4 für die neue Regel inkl. 1-Tages-Edge-Case).

**Scroll-Reset + Compressor-Toggle (2026-05-04 b):** Zwei kleinere UX-Korrekturen. **Scroll-Reset:** beim Tab-/View-Wechsel startet der neue Inhalt oben — außer für die Shopping-Tabs (Cairns/Bamaga/Cooktown/Coen/Archer), die ihre individuelle scroll-Position pro Tab in einer Session-Map merken (Menu → Cairns@200 → Bamaga → Cairns liefert wieder 200). View-Wechsel (Home/Configurator/Trip-Active/About) immer oben. Implementiert via `useRef` auf `.content` plus `scrollPositionsRef`-Map in `App.jsx`. **Compressor-Toggle:** neue Pill „Compressor fridge?" (Yes/No) im Configurator nach „Fridge size". Compressor=Yes entfernt das „Ice"-Item komplett aus Cairns- und Mid-Stop-Buckets; Compressor=No skaliert Eis mit Fridge-Größe (large/medium = 2× 5kg in Cairns, small = 3× 5kg). Vorher war Compressor implizit aus `fridgeSize === 'large'` abgeleitet — fehleranfällig (große Cooler-Boxen, kleine Engels). Storage-Migration: alte Saves ohne das Feld bekommen `fridgeCompressor = (fridgeSize === 'large')` als Backward-Compat-Default. Test-Total 142 → 143.

**Onboarding-UX: Range-Select-Calendar (2026-05-04):** Erstaufruf-Flow runderneuert. **`defaultConfig()`** liefert jetzt `days: 0` und `startDate: null` als Sentinel-Werte für „kein Trip gewählt" → HomeTab zeigt Empty-State mit „Plan your Cape York trip"-CTA, Configurator zeigt **leeren Calendar im Range-Select-Mode**. **`TripCalendar`** umgebaut: 1. Tap = pendingStart (orange Highlight via neue `.cal-pending`-Klasse), 2. Tap = onSelectRange-Callback mit berechneten `days` (auto-swap bei End < Start, gleicher Tag = 1-Tages-Trip). View-Mode (Range gewählt): Tap auf Trip-Tag öffnet DaySheet, Tap außerhalb wird ignoriert (kein versehentliches Re-Picken mehr). **Days-Stepper komplett raus** im Configurator; stattdessen read-only Hint („14 days selected" / „Tap a start day, then end day") + **„↺ Re-pick dates"-Button** der die Range zurücksetzt. Submit-Button (Generate/Update) ist disabled bis `hasRange`. **Trip-Länge erweitert von [7, 28] auf [1, 31]** — Generator-Klemmung in `lib/generator.js:679` runter, deckt Tagesausflug bis Monats-Trip ab. **Stop-Filterung nach `minTripDays`** in `regions.js`: Cooktown ab 2 Tagen, Coen ab 4, Archer/Bamaga ab 5 — Stops, deren Min nicht erreicht ist, verschwinden aus DaySheet-Toggles, Calendar-Markern und Tabs. `handleSelectRange` setzt bei Trip-Verkürzung ggf. `bamagaStop=false` und `enabledStops[id]=false` automatisch (sonst routet Generator Frisch-Items in unsichtbaren Bucket). 142 Tests grün (vorher 141, +1 für 3-Tages-Edge-Case). Bundle 273.45 → 274.64 kB JS (+1.2 kB).

**Privacy Policy auf GitHub Pages gehostet (2026-05-03):** Der letzte Pre-Submission-Block für Google Play geschlossen. **`scripts/build-privacy.mjs`** (neu) konvertiert `PRIVACY.md` → `public/privacy.html` mit minimal inline-styling (Cape-York-Orange-Akzent, mobile-friendly, ohne externe Dependencies). Hookt sich an den **`prebuild`**-npm-Hook → läuft automatisch bei jedem `npm run build` (also auch im CI). `public/privacy.html` ist gitignored — Source of Truth bleibt PRIVACY.md, HTML wird im CI fresh generiert. Stabile Pages-URL: **https://japeyer.github.io/cape-york-app/privacy.html**. AboutTab-Link aktualisiert (war GitHub-Repo-URL → ist jetzt die hosted Pages-URL — Play-Store-tauglich, kein Login-Wall, stabil über Releases). Neue devDep: `marked@^18` (~50 kB als devDep, nicht im Production-Bundle). Bundle/Tests unverändert (273.44 kB JS / 86.22 kB gzip · 141 grün) — privacy.html zählt nicht zum Bundle-Size weil es ein statisches File ist.

**Pre-Play-Store-Block: Privacy + About-Page (2026-05-03):** Erster konkreter Schritt Richtung Play-Store-Submission. **`PRIVACY.md`** im Repo (englisch, 10 Sektionen — was die App handhabt, wo es liegt, wer es kriegt (niemand), Network-Calls, Cookies, Children, Lösch-Workflow, OSM-Attribution, Änderungs-Policy, Kontakt). **`DATA_SAFETY.md`** als interne Audit-Doku (deutsch, vorbereitete Antworten für die Play-Store-Data-Safety-Form, vollständiges localStorage-/Network-/Permissions-Inventar, Drittanbieter-Library-Audit). Audit-Ergebnis: **0 Network-Calls in src/, 0 Cookies, 0 Tracker, 0 Drittanbieter-CDNs** — saubere Privacy-Story (alles localStorage, alles offline). **`AboutTab.jsx`** als neue View in der State-Machine (`view='about'`), erreichbar via **ⓘ-Icon im Topbar** (nur auf Home-View, sonst Topbar überladen). Sektionen: App-Header (🦘 + Tagline), „Your data" (kurz: alles bleibt lokal) mit Link zur PRIVACY.md, „Data sources" (OSM-Snapshot-Datum + ODbL-Attribution sichtbar), „Important" (Disclaimer mit Orange-Akzent: planning aid, nicht ersatz für Trip-Vorbereitung; OSM-Daten können outdated sein; Allergen-Filter ist Heuristik), „Contact" (Email + Source-Link). Bundle: 270.28 → 273.45 kB JS (+3.2 kB), CSS 24.46 → 25.74 kB.

**Fuel-Section im DaySheet (2026-05-03):** OSM-Daten erstmals UI-sichtbar. Tap auf einen Trip-Tag im Calendar zeigt jetzt nicht nur Resupply-Stops + Restaurant-Toggles, sondern auch eine **„Fuel ahead"-Section** mit den nächsten 3 Tankstellen auf der Strecke. Logik in `src/lib/route-position.js` (neu): Cairns (km 0, Tag 1) und Bamaga (km 1000, bamagaDay) als Anker, lineare Interpolation für die km-Position pro Tag, plus Richtungs-Flag (north outbound / south return). Section nur wenn `bamagaStop=true` (ohne Norden-Anker keine sinnvolle Schätzung). Hint zeigt geschätzte km-Position („Estimated position: ~500km from Cairns, heading north"); pro Stop: Name + Brand · km-Marker + Diesel-Confirmed-Badge wenn OSM-tagged. ODbL-Attribution unten in der Section. (unnamed fuel)-Cairns-Metro-Stations werden ausgefiltert. 16 neue Tests in `route-position.test.js` → Test-Total **141** (vorher 125). Bundle 270.28 kB JS / 85.26 kB gzip · CSS 24.46 kB / 4.80 kB gzip.

**OSM-Fuel-Stop-Datenpipeline (2026-05-03):** Build-time Script (`scripts/fetch-osm.mjs` → `npm run osm:refresh`) hits Overpass API, extrahiert **71 Tankstellen** in Cape-York-BBox, davon **15 nördlich von Cooktown** (Hann River/Musgrave/Coen/Archer/Bramwell/Bamaga). Output: `src/data/route-pois.js` (auto-generiert, ODbL-attribuiert). **kmFromCairns-Werte aus `regions.js` verifiziert**: Cairns/Coen/Archer/Bamaga in 30 km Toleranz; Cooktown -108 km Diff weil Mulligan-Highway-Route deutlich kurviger als 1.30× Luftlinie ist (330 km bleibt korrekt). Bewusst Build-time statt Runtime-Fetch — App muss offline-fähig bleiben (Cape York hat oft keinen Empfang). 11 neue Tests in `route-pois.test.js` (Datenvertrag + Plausibilität + ODbL-Compliance) → Test-Total **125** (vorher 114). UI konsumiert die Daten noch nicht — Foundation für künftige Fuel-Stop-Komponenten (Map-Tab / „nächste Tankstelle"-Card / Daysheet-Hint).

**Konzept-Pivot abgeschlossen** (siehe `PRODUCT.md` und `CLAUDE.md`): Produkt zielt jetzt auf Cape-York-4WD-Mieter aus Cairns als breite Zielgruppe; eigener Juni-2026-Trip ist Dogfood-Test, nicht primärer Use-Case. Konfigurator-basiert (Tage 7–28, Gruppe 1–8 Personen mit individueller Charakterisierung, Diät), englische UI, Plan + Einkaufsliste werden generiert statt statisch ausgeliefert.

**Calendar-only Stop-Management (2026-05-02):** Stop-Aktivierung läuft jetzt **ausschließlich über den Calendar**. Bamaga-PillPicker (Yes/No) und die Cooktown/Coen/Archer-Toggle-Pills sind raus aus dem Configurator. Tap auf einen Trip-Tag → DaySheet zeigt **alle 4 optionalen Stops** als Toggle-Rows (auch inaktive). Tap auf inaktiven Stop → enable + assign auf diesen Tag. Tap auf aktiven Stop am gleichen Tag → disable. Tap auf aktiven Stop an anderem Tag → move auf diesen Tag (Label zeigt aktuellen Tag: „Bamaga arrival · currently day 9 — tap to move here"). Day 1 (Cairns-Start) und letzter Tag verstecken die Resupply-Section komplett (kein sinnvoller Stop dort). Neuer **First-Time-Tipp** in einer orange Hint-Box direkt unter dem Calendar: „💡 Tap any trip day to plan eating out or mark a resupply stop (Bamaga, Cooktown, Coen, Archer River)." Datenmodell unverändert — `bamagaStop`/`bamagaDay`/`enabledStops`/`stopDays` werden weiter geschrieben, nur die UI-Affordance hat sich geändert.

**Home-Screen + View-State-Machine (2026-05-02):** Erstaufruf landet jetzt auf einer **Startseite** statt direkt im Configurator. Drei Views: `home` (Trip-Card mit Open/Edit, oder Empty-State mit Create-CTA) · `trip-config` (Configurator full-screen, ohne Bottom-Nav) · `trip-active` (Menu/Recipes/Shopping-Tabs ohne Trip-Tab). Configurator-Tab ist absichtlich **nicht** mehr in der Tab-Leiste — Trip-Edit nur via „Edit trip" auf der Home-Card → Trip fühlt sich „definitiver" an, Listen wirken verlässlicher (keine versehentlichen Re-Generierungen durch Tab-Klicks). Topbar bekommt Back-Button (←) wenn `view !== 'home'`. „Reset & start a new trip" auf Home triggert Confirm + voller Wipe + Configurator-Onboarding.

**MVP-Roadmap (Stufe 1) durch + Kalender + Rezept-Tausch:** Configurator hat jetzt einen **Outlook-Style Kalender** als primäres Datums-UI (Days-Stepper bleibt für Länge, Calendar zeigt Trip-Range orange + erlaubt Start-Verschiebung per Tap). Tap auf einen Trip-Tag öffnet Bottom-Sheet mit Bamaga-Toggle + Restaurant-Toggles für Frühstück/Lunch/Dinner. Bamaga-Stepper ist raus (Bamaga-Tag wird im Kalender gewählt). Restaurant-Mahlzeiten erscheinen im MenuTab als „🍽 Restaurant" und reduzieren die Einkaufsliste entsprechend. **Rezept-Tausch:** ⟳-Button pro Mahlzeit im MenuTab öffnet Bottom-Sheet mit kompatiblen Rezepten aus dem Gesamt-Pool; getauschte Mahlzeit kriegt grünes „Manually picked"-Tag und überlebt jeden Re-Generate (auch bei Diät-Wechsel). Storage erweitert um `startDate`, `restaurantSlots`, `overrides` mit Migration für alte Saves. Multi-Monats-Trips werden untereinander gescrollt (Mobile-360px-tauglich).

Zusätzlich auch unverändert verfügbar:**MVP-Roadmap (Stufe 1) durch + Fleisch-Clustering + Allergie-Filter:** Schritte 1–6 abgeschlossen, dazu Equipment-Toggles, Pool 24→51, Per-Person-Personalisierung, Bamaga-Stop konfigurierbar, Fleisch-Clustering — und jetzt **Allergien-/Präferenzen-Filter** (8 gruppen-weite Toggles: Nüsse, Gluten, Milchprodukte, Eier, Soja, Fisch, Schalentiere, Schwein). Configurator hat einen neuen „Allergies or preferences? Yes/No"-Block; bei Yes erscheinen 8 Toggle-Pills (rote Akzentfarbe statt orange). 2-stufige Filter-Logik im Generator: Rezepte mit Core-Allergen werden komplett ausgeschlossen; Rezepte mit Topping-Allergen (z.B. „Nuts on the side") landen ans Ende des Pools und werden nur bei Wraparound gepickt — plus rotem ⚠-Banner an Mahlzeit/Rezept. Heuristik in `lib/allergens.js` ohne per-Rezept-Tags (Plant-Milks/Plant-Butters/`*-free` False-Positives gefiltert). Build grün (230.69 kB JS / 73.78 kB gzip · CSS 13.40 kB / 3.00 kB gzip). Capacitor bleibt Stufe 2.

## Funktioniert ✓

- React 18.3 + Vite 5.4 + vite-plugin-pwa 0.20
- `npm install` erfolgreich (356 Pakete, `package-lock.json` existiert)
- `npm run build` läuft in ~500 ms durch
- Bundle: 185.86 kB JS / 59.22 kB gzip · 7.39 kB CSS / 1.99 kB gzip
- PWA-Pipeline generiert `sw.js`, `workbox-*.js`, `manifest.webmanifest`
- **9 Precache-Entries (193 KiB)** inkl. Icons
- Manifest: Theme `#C0600C`, Background `#F2EDE7`, `display: standalone`, `orientation: portrait`

## Code vorhanden (post Per-Person-Personalisierung)

> **End-to-End konfigurator-getrieben mit Home-Startseite.** Erstaufruf landet auf der `HomeTab` (Empty-State mit „Plan your Cape York trip"-CTA wenn kein Trip existiert). CTA wechselt in den Configurator (`view='trip-config'`); nach „Generate plan" wechselt App in `trip-active` mit Bottom-Nav (Menu/Recipes/Shopping-Tabs). Spätere Edits regenerieren Plan + Einkaufsliste *nicht-destruktiv* — abgehakte Items, die nicht mehr im Plan stehen, landen als „Already bought (from previous plan)" in der ShoppingTab. Trip-Edit ist nur via „Edit trip" auf der Home-Card erreichbar (kein Trip-Tab in der Bottom-Nav).

- `src/main.jsx`, `src/App.jsx` — **View-State-Machine** mit drei States: `home` / `trip-config` / `trip-active`. Bottom-Nav nur in `trip-active` gerendert; Tabs dort: 📅 Menu · 👨‍🍳 Recipes · 🏪 Cairns · 🌿 Bamaga + 🏪 Cooktown / ⛽ Coen / 🏕 Archer (**je nur wenn aktiviert**). Configurator-Tab bewusst **nicht** in der Tab-Leiste — Trip-Edit nur via Home → „Edit trip"-Button. App.jsx hält Config-State, computet Generator-Output via `useMemo` (Dependency-Hash über `JSON.stringify(config.people)` damit nicht jeder unrelated Render neu triggert), zeigt Warnings-Banner über dem aktiven Content nur in `trip-active`. **Topbar dynamisch:** Back-Button (←) erscheint wenn `view !== 'home'`; Title wechselt zwischen App-Titel (Home), „New trip"/„Edit trip" (Configurator) und Trip-Default-Name (Trip-Active). Subtitle ist Home-Subtitle, Edit-Hint oder Trip-Summary. **Callbacks:** `handleOpenTrip` (Home → Trip-Active), `handleEditTrip` (Home → Configurator), `handleBackHome` (Trip-Active/Configurator → Home), `handleConfigSubmit` (Configurator → Trip-Active mit fixiertem Plan), `handleCreateNew` (mit Confirm wenn Trip existiert → `resetAll`). `useEffect`-Fallback: war `activeTab` ein Supply-Tab und der User schaltet ihn aus, schnappt der Tab auf `menu` zurück. CSS-Klasse `app-no-nav` wenn `view !== 'trip-active'` damit Content-Bottom auf 0 statt nav-height geht.
- **`src/components/HomeTab.jsx`** (neu, 2026-05-02) — Startseite mit zwei Modi: **Empty-State** (`!config.completed`): 🦘-Icon + „No trip planned yet"-Headline + „Plan your Cape York trip"-CTA. **Trip-Card** (mit `completed`): Section-Label „Your trip" + Card mit Trip-Name + Datums-Range („Sunday, June 14, 2026 → Monday, June 29, 2026") + Summary-Text + „Open →"-CTA. Daneben „Edit trip"-Button (✎). Unten: Divider + sekundärer „Reset & start a new trip"-Button mit Hint-Text (warnt vor Wipe). Datum-Format via `S.config.calendar.weekdaysFull` + `monthNames`. Props: `config`, `summary`, `onOpenTrip`, `onEditTrip`, `onCreateNew`.
- **`src/lib/allergens.js`** (neu, 2026-05-01) — Allergen-Detection auf Zutaten-Ebene. Konstanten: `ALLERGENS = ['nuts','gluten','dairy','eggs','soy','fish','shellfish','pork']`, pro Allergen eine Vokabel-Regex. API: `recipeAllergenStatus(recipe, allergen)` → `'core'|'topping'|undefined`, `recipeAllergens(recipe)` → Map allergen→status, `filterByAllergens(recipe, userAllergens)` → `{keep, toppingAllergens}`. Topping-Marker: `Optional:`/`If using:`/`to serve`/`to taste`/`to top`/`to garnish`/`on the side`/`(optional)`/`if you have`. False-Positive-Filter: `<allergen>-free`-Patterns (z.B. „egg-free wheat noodles"), `PLANT_MILK_RX` (Coconut/Oat/Almond/Soy/Rice milk → kein dairy), `PLANT_BUTTER_RX` (Peanut/Almond/Coconut butter → kein dairy).
- **`src/components/ConfiguratorTab.jsx`** — Stepper für Days (7–28). **`GroupEditor` mit `PersonRow`-Sub-Komponente**: pro Person 3 Type-Pills (Man / Woman / Child) + **4 Appetite-Pills** (Light / Medium / Heavy / **Custom**) + ✕-Remove (deaktiviert wenn nur 1 Person übrig). Add-Person-Button bis max. 8. **Custom-Modus**: Klick auf „Custom" klappt Stepper aufgespannt unterhalb auf (±100 kcal, Range 1500–4500); Initialwert ist der gerundete Medium-kcal des aktuellen Types (Adult-M → 2800), Vorwert wird beim Toggle-back nicht verloren. Pro-Person-Kcal-Anzeige (`≈ X kcal/day`) und Gruppen-Total-Footer (`Daily target: Y kcal/day`). Drei Pill-Picker für Diät / Burners / Fridge-Size. **Bamaga-Stop-Picker (Yes/No)** — bei Yes klappt zusätzlicher Stepper für `Bamaga arrival day` auf (Range 2 bis `days−1`, Tag 1 ist immer Cairns-Start). Days-Änderung clamped `bamagaDay` automatisch, damit der Stepper nie out-of-range steht. Generischer `PillPicker`-Helper. „Generate plan" / „Update plan" CTA. Mobile-first, Touch-Targets ≥44 px.
- `src/components/MenuTab.jsx` — nimmt `plan`-Prop, dynamische Phase-Header (`Days 1–9` / `Days 10–16` aus Bamaga-Index). Restaurant-Summary-Cells bleiben (für künftige Restaurant-Slots). Kennt `persons` nicht mehr — wurde nicht gebraucht.
- `src/components/ShoppingTab.jsx` — nimmt `data`-Prop (= `result.shopping[supplyPoint.id]`). Orphan-Sektion „Already bought (from previous plan)" listet Storage-Keys, die nicht mehr in der aktuellen Liste sind; Tap löscht den Eintrag. Plan-Identitäts-Key (sortierter ID-Hash) triggert State-Refresh aus localStorage bei Re-Generation.
- `src/components/RecipesTab.jsx` — nimmt `plan`-Prop, filtert `RECIPES` auf IDs, die im aktuellen Plan vorkommen (Set-basiert). Leere Kategorie-Sektionen werden ausgeblendet, Empty-State (`S.recipes.empty`) wenn nichts. Ingredients-Label dynamisch via `S.recipes.ingredients({ persons })` (= `result.config.persons` = `people.length`).
- **`src/lib/generator.js`** — `generate({days, people, diet, burners, fridgeSize, bamagaStop, bamagaDay})` → `{config, plan, shopping, warnings}`. Skaliert die Einkaufsliste über einen **reellen `groupFactor`** statt Integer-Persons (1 Adult-Mann Medium = 1.05; 1M+1F Medium = 2.00 → backward-compat zum alten `persons:2`; Familie 2A+2K = 2.99). `scaleFactor(parsed, factor)` rechnet dezimal hoch und rundet erst beim Display. **Bamaga-Routing**: `bamagaStop === false` → kein Tag bekommt das `bamaga`-Flag, alle frischen Items landen in Cairns, `result.shopping.bamaga` wird gelöscht. `bamagaStop === true` → `bamagaDay` wird auf [2, days−1] geklemmt; Frischfleisch/Gemüse/Frucht für Tage *nach* dem Bamaga-Tag wandern in den Bamaga-Bucket. **Fleisch-Clustering** (neu, 2026-05-01): Plan-Pool wird via `buildSplitPool` in `meat`/`nonMeat` gesplittet. `meatClusterDays(fridge, groupF)` rechnet `floor(fridgeMeatVolume / (groupF × 500mL))` clamped auf [1, 6] (Fridge-Vorrat: small=3 L / medium=6 L / large=12 L). Cluster-Zonen post-Cairns + post-Bamaga via `meatDayIndex`. Im Cluster sortiert `shelfPreference(idx)` Fleisch-Picks nach Haltbarkeit (idx 0–1: short bevorzugt = Geflügel/Hack; idx 4+: long zuerst = Steak/Bacon). Frühstück/Lunch: max 1 Fleisch je Cluster, an Cluster-Tag 0 (für Frühstück) bzw. 0–1 (für Lunch). Outside-Cluster-Tage = `nonMeat`-Pool, also vegetarisch/vegan auch bei Omnivore-Diät. Helpers `containsFreshMeat` und `meatShelfLife` sind exportiert (Optional/If-using-Zutaten ignoriert, shelf-stable wie jerky/canned ausgeschlossen). `config` im Output: `days`, `people`, `persons`, `groupFactor`, `dailyKcal`, `diet`, `dietApplied`, `burners`, `burnersApplied`, `fridgeSize`, `bamagaStop`, `bamagaDay`, **`meatClusterDays`** (neu). Warning-Klassen: Diät-Fallback / Burner-Fallback / Thin-Pool / **Few-meat-free-options** (neu) / **Fridge-cluster-info** (neu, ersetzt alte Small-Fridge-Warnung).
- **`src/lib/calories.js`** — Single-Source für Gruppen-Skalierung. Konstanten: `TYPES = ['adult-m', 'adult-f', 'child']`, `APPETITES = ['light', 'medium', 'heavy', 'custom']`, `TYPE_FACTOR = { 'adult-m': 1.05, 'adult-f': 0.95, 'child': 0.55 }`, `APPETITE_FACTOR = { light: 0.80, medium: 1.00, heavy: 1.20 }`, **`BASE_KCAL = 2700`** (von 3000 gesenkt für realistische Aktiv-Outdoor-Tage), `CUSTOM_KCAL_MIN/MAX/STEP = 1500/4500/100`. Helpers: `personFactor(p)` (im Custom-Modus = `customKcal / BASE_KCAL`, sonst Type×Appetite), `personDailyKcal(p)` (im Custom-Modus = `customKcal`), `groupFactor(people)`, `groupDailyKcal(people)`, `clampCustomKcal(v)` (auf 100er-Schritt + Range-Clamp), `roundToHundred(n)`, `migratePersonsToPeople(persons)` (1→1M, 2→1M+1F, 3→1M+1F+Child, 4→1M+1F+2C, 5+→1M+(N−1)F), `makePersonId(seed)`. `BASE_KCAL × groupFactor` ist der Tageskalorien-Bedarf der Gruppe; im Generator wird derselbe Faktor zum Skalieren der Mengen verwendet. **Werte:** Adult-M Light/Medium/Heavy = 2268/2835/3402, Adult-F Medium = 2565, Child Medium = 1485.
- `src/strings.js` — User-facing Strings unter `S.app / S.config / S.menu / S.recipes / S.shopping`. Neu: `S.config.typeOptions` (Mann/Frau/Kind mit Icon), `S.config.appetiteOptions` (Light/Medium/Heavy/Custom mit Sub-Label), `S.config.customKcalUnit` (`kcal/day`), `S.config.groupLabel` / `groupHint` / `addPerson` / `dailyKcalLabel` / `removePerson` / `perPersonKcal`. `summary`-Template um `dailyKcal`-Parameter erweitert (`16 days · 2 people · Omnivore · 5400 kcal/day`).
- `src/data/regions.js` — Cape-York-Metadaten und `supplyPoints`-Array (Cairns + Bamaga). Datengetriebene Single-Source für Versorgungspunkte.
- `src/data/packs.js` — **`PACK_SIZES`** (Stufe-2-Waste-Optimierung): typische AU-Packungsgrößen (~20 Zutaten) mit `pack`/`unit`/`mainStaple`/`perishableOpen`/`contains`. Steuert Pack-Rundung in der Einkaufsliste + Pack-Fill-Bias im Generator. Keys = `canonicalIngredient().key`.
- `src/data/recipes.js` — **97 Rezepte** (Pool laufend erweitert; ältere „51"-Angabe war stale) (10 Frühstücke / 17 Lunches / 24 Dinners). Diät-Verteilung: 18 omnivore, 12 vegetarian, 11 vegan. Burner-Verteilung: 38 single-burner, 13 two-burner. Kritische Pool-Sizes: vegan-1-burner = 4f/5m/5a, vegetarian-1-burner = 7/11/8. Tags `diet`/`cooling`/`effort`/`burners`. Mengen pro-Person — `lib/generator.js` skaliert mit Real-Number-Faktor statt Integer-Persons.
- `src/hooks/useStorage.js` — `getChecked` / `setChecked` / `resetPrefix` / `getAllCheckedIds(prefix)` (für Orphan-Detection) / `loadConfig` / `saveConfig` (`cfg_v1`). DEFAULT_CONFIG: 16 Tage / `people: [Adult-M-Medium, Adult-F-Medium]` (= Faktor 2.00 = backward-compat zu `persons:2`) / omnivore / 2 Burners / large Fridge / **`bamagaStop: true`, `bamagaDay: 9`**. **Migration**: alte `cfg_v1` mit `persons:N` werden via `migratePersonsToPeople(N)` automatisch in `people`-Array überführt; alte ohne `burners`/`fridgeSize` bekommen Defaults beim Merge; alte ohne `bamagaStop` bekommen `true` plus geklemmten `bamagaDay = round(days × 0.55)`.
- `src/App.css` (12.6 kB) — neu: `.person-row` (Card-Look pro Person), `.person-head` (Index + Pro-Person-Kcal + Remove-✕), `.person-num`, `.person-kcal`, `.person-remove`, `.person-grid` (3-Spalten-Pill-Grid für Type), **`.person-grid-4`** (4-Spalten-Variante für Light/Medium/Heavy/Custom mit kompakteren Labels), `.person-pill` mit `-icon` / `-label` / `-sub`, **`.custom-kcal`** (Stepper-Box mit Orange-Border + Hintergrund), **`.custom-kcal-btn`** (36×36 Plus/Minus mit Disabled-State), **`.custom-kcal-val`** (zentrierter kcal-Wert), `.add-person-btn` (Dashed-Border-Style), `.group-total` (Footer mit Daily-Target-Total). Bottom-nav auf `grid-auto-flow: column` (skaliert mit Tab-Anzahl).
- `index.html` mit Mobile/PWA-Meta-Tags, `lang="en"`
- `vite.config.js` PWA-Manifest-Description englisch

## Assets vorhanden

- `public/icon-192.png` — **„looks-real"-Platzhalter** (orange Brand-Bg, weißer „CY" + Trennstrich + „2026", umrahmender Kreis). Generierbar via PowerShell-Script (siehe Tech-Notizen). Tausch gegen finales Logo durch reines Datei-Überschreiben — kein Code-Change.
- `public/icon-512.png` — gleiche Optik, Hi-Res-Version für Splash-Screen + Play-Store.

## Offen / bekannte Lücken

| # | Punkt | Anmerkung |
|---|---|---|
| 1 | MVP-Schritte 1–6 + Equipment-Toggles + Per-Person-Personalisierung durch | App ist konfigurator-getrieben (Tage / Gruppe-mit-Type+Appetit / Diät / Burners / Fridge), Generator-Output live im UI. Polish-Punkte siehe unten. |
| 2 | Pool-Erweiterung erledigt | Pool von 24 → 51 Rezepte. Vegan-1-Burner: 0 → 5 Dinners. Vegetarian-1-Burner: 1 → 8 Dinners. Thin-Pool-Warnings feuern jetzt nicht mehr für sinnvolle Configs. Bei sehr restriktiven Trips (vegan + 28 Tage + 1-Burner) gibt es noch Wiederholungen — Stufe-2-Algorithmus könnte das durch echte Abwechslung-Logik lösen. |
| 2a | Kalorien-Faktoren sind Heuristik | `lib/calories.js`-Faktoren (1.05/0.95/0.55 bzw. 0.8/1.0/1.2) sind Schätzwerte um `BASE_KCAL = 2700`. Adult-M Medium = 2835 kcal — realistisch für 80 kg / aktiv-Outdoor. Wer abweicht, nutzt **Custom-Modus** pro Person (±100-kcal-Stepper, 1500–4500). Faktoren zentral justierbar wenn Eigen-Trip andere Werte zeigt. |
| 3 | Pool-Monotonie bei sehr langen / strikten Trips | Bei vegan + 28 Tage werden Dinners ~5× wiederholt (28/5). Akzeptabel für MVP, Stufe-2-Algorithmus würde das durch Abwechslung-Heuristik (z.B. „kein Rezept zwei Tage in Folge") oder grösseren Pool ändern. |
| 4 | Equipment-Dataset klein | Burner + Fridge-Tags basieren auf 6 Inventuren von 3 Vermietern. Patterns wirken stabil, aber Outlier (Wok / Oven / Premium-Glamping) nicht abgedeckt. Erweiterbar auf neue Toggles wenn neue Equipment-Klassen auftauchen. |
| 5 | ~~Camping-Essentials nicht in Liste~~ ✓ erledigt (2026-05-02) | `region.essentials` in `regions.js` + `injectEssentials` im Generator. Cairns 15 Items, Bamaga 4 Refills. Wasser/Eis skalieren mit Personen/Tagen/Fridge. |
| 6 | Icons sind „looks-real"-Platzhalter | CY26-Wortmarke auf orange, mit Kreisrand. Brauchbar für Pitch + Werbevideo, aber kein finales Logo. Tausch durch reines Überschreiben von `public/icon-192.png` und `public/icon-512.png`. |
| 7 | npm-Vulnerabilities | 6 (2 moderate, 4 high), alle transitive **Dev-Deps** (Vite/Workbox). Kein Runtime-Risiko. `npm audit fix --force` würde Breaking Changes einspielen → bewusst nicht ausgeführt |
| 8 | Capacitor | Laut `CLAUDE.md` Stufe 2, noch nicht installiert |

## Nächste sinnvolle Schritte (priorisiert)

> Reihenfolge orientiert sich an `PRODUCT.md` § 3 Top-5 und an `CLAUDE.md` § Architektur-Vorgaben. Vollständige Mid-/Long-Term-Roadmap in `CLAUDE.md` § Roadmap.

**Erledigt:**
- ✓ **Schritt 1: Live-Test des Ist-Stands** — `npm run dev` läuft auf `http://localhost:5173/`, alle Tabs klickbar, `localStorage`-Persistenz nach Reload bestätigt.
- ✓ **Schritt 2: Strings auslagern** — `src/strings.js` angelegt, `App.jsx` / `MenuTab` / `RecipesTab` / `ShoppingTab` / `index.html` / `vite.config.js` auf Englisch + `S`-Imports umgestellt. HMR-bestätigt im Browser.
- ✓ **Schritt 3: Rezepte mit Tags + Übersetzung** — `src/data/recipes.js`: alle 24 Rezepte auf Englisch, neue Tags `diet` / `cooling` / `effort` pro Rezept; `catLbl`-Feld entfernt (kommt nun aus `S.recipes.sections`). HMR-bestätigt.
- ✓ **Schritt 4: Versorgungspunkte datengetrieben + restliche Daten übersetzt** — `src/data/regions.js` neu (Cape York: Cairns + Bamaga). `shopping.js` umgebaut zu `SHOPPING_LISTS = { cairns: [...], bamaga: [...] }`, übersetzt. `days.js` übersetzt + Bug-Fix für vertauschte Recipe-Links Tag 14/15. `App.jsx` generiert Tabs aus `REGION.supplyPoints`, `ShoppingTab.jsx` nimmt `supplyPoint`-Objekt statt `list`-String. Strings.js Tab-Labels für Cairns/Bamaga entfernt (jetzt aus `supplyPoint.name`).
- ✓ **Schritt 5: Generator-Logik gebaut** — `src/lib/generator.js` neu. API: `generate({days, persons, diet})` → `{config, plan, shopping, warnings}`. Deterministisch: Round-Robin pro Kategorie aus dem Diät-gefilterten Pool, sortiert nach Kühlbedarf (high → low) so dass frische Mahlzeiten am Trip-Anfang landen; Bamaga-Tag bei ~55% der Strecke. Heuristischer Zutaten-Parser (Range-Mittelwert, `/person` × Personen, `(for both)` × Personen/2, Container-Hint), Aggregation nach Einheits-Klasse mit g/kg- und ml/L-Auto-Switch, Display-Names ohne Parenthetik. Bamaga-Bucket bekommt frisches Fleisch / Gemüse / Frucht für Tage *nach* dem Bamaga-Tag, alles andere bleibt in Cairns. Vegan-Fallback auf vegetarisch mit `warnings`-Eintrag (Seed-Pool hat 0 vegane Rezepte). Smoke-Test über 4 Konfigurationen (16/2/omni, 7/1/veg, 28/8/omni, 10/4/vegan) bestanden, `npm run build` grün.
- ✓ **Schritt 6: Konfigurator-Tab + UI-Verkabelung** — `ConfiguratorTab.jsx` neu (Stepper + Diet-Pills + Generate-CTA, Onboarding- vs. Edit-Variante). `App.jsx` hält `cfg_v1`-State, computet Generator-Output via `useMemo`, zeigt Warnings-Banner, Subtitle dynamisch. `MenuTab` nimmt `plan`-Prop mit dynamischen Phase-Headern (Bamaga-Index → `Days 1–9` / `Days 10–16`). `ShoppingTab` nimmt `data`-Prop und detect-iert Orphans (markiert in `localStorage`, aber nicht mehr im Plan) → eigene Sektion „Already bought (from previous plan)" zum Tap-Clearen. `RecipesTab.ingredients`-Label dynamisch via `S.recipes.ingredients({ persons })`. `useStorage.js` um `getAllCheckedIds(prefix)` / `loadConfig` / `saveConfig` erweitert. Item-IDs im Generator ohne Bucket-Prefix → Storage-Keys stabil bei Cairns↔Bamaga-Wechsel. **Statische `days.js` + `shopping.js` gelöscht** (pre-pivot-Inhalt obsolet). Build grün (187 kB JS / 61 kB gzip · CSS 10 kB).
- ✓ **Equipment-Toggles** — Burners (1/2) + Fridge-Size (small/medium/large) als Pill-Picker im Konfigurator. Generator filtert mit Burner-Fallback + Thin-Pool-Warnings.
- ✓ **Pool-Erweiterung 24 → 51 Rezepte** — Schwerpunkt vegane / vegetarische / Single-Burner-Optionen. Vegan-1-Burner geht jetzt ohne Fallback (4f/5m/5a). categorize-Regex und FRESH_CATEGORIES für vegane Vokabeln (tofu/hummus/maple syrup etc.) erweitert.
- ✓ **Per-Person-Personalisierung (Geschlecht / Alter / Appetit)** — `lib/calories.js` neu (TYPE_FACTOR × APPETITE_FACTOR × `BASE_KCAL`). `ConfiguratorTab.GroupEditor` ersetzt Persons-Stepper durch dynamische Person-Rows mit Type-Pills (Mann/Frau/Kind) + Appetite-Pills + Add/Remove. Pro-Person-Kcal-Anzeige + Gruppen-Total-Footer. Generator skaliert Mengen mit reellem `groupFactor` statt Integer-Persons; `result.config.dailyKcal` zeigt Tagesbedarf in der Topbar-Subtitle. `useStorage.js` migriert alte `cfg_v1` mit `persons:N` automatisch zu `people`-Array.
- ✓ **Kalorien-Kalibrierung + Custom-Modus** — `BASE_KCAL` von 3000 auf **2700** gesenkt (entspricht aktiver Outdoor-Tag für ~80 kg Erwachsenen ohne Über-Schätzung). Adult-M Medium nun 2835 statt 3150, Heavy 3402, Light 2268 — realistisch laut TDEE. **Vierter Appetite-Modus „Custom"** pro Person: ±100-kcal-Stepper mit Range 1500–4500, Initial-Wert aus aktuellem Type+Medium gerundet, Vorwert bleibt erhalten beim Toggle. `personFactor`/`personDailyKcal` schalten im Custom-Modus auf direkten kcal-Wert um. **`groupFactor` der Defaults bleibt unverändert** (1M+1F Medium = 2.00 vorher und nachher) → Mengen-Skalierung der Einkaufsliste backward-compat. Smoke-Test 8 Per-Person + 5 Group + Clamp/Round-Helpers grün, Build 221 kB JS / 70.58 kB gzip · CSS 12.58 kB / 2.87 kB gzip.
- ✓ **Bamaga-Stop konfigurierbar** — Generator-API erweitert um `bamagaStop` (Yes/No) + `bamagaDay` (Tag 2 bis days−1, Tag 1 ist immer Cairns-Start). Bei No: `result.shopping.bamaga` wird gelöscht, alle Items routen nach Cairns; bei Yes: Frisch-Splitting bei dem vom User gewählten Tag statt 55%-Heuristik. **Bamaga-Tab in `App.jsx`** filtert sich aus den Tabs raus wenn `bamagaStop === false`; `useEffect` snappt `activeTab` zurück auf `menu` falls der User den Bamaga-Tab gerade ausschaltet. Configurator zeigt Pill-Picker (Yes/No) + konditionalen Stepper. Storage-Migration: alte `cfg_v1` ohne `bamagaStop` bekommen `true` plus `round(days × 0.55)` als Tag-Default. Smoke-Test 9 Cases (default, explicit day 9/5/14, no-stop, day 1 → 2 clamp, day 99 → days−1 clamp, kurze 7-Tage-Trips mit/ohne) grün. Build 223 kB JS / 71.10 kB gzip · CSS unverändert 12.58 kB / 2.87 kB gzip.
- ✓ **Fleisch-Clustering nach Kühlschrank-Volumen + Shelf-Life-Sortierung** (2026-05-01) — Generator unterscheidet jetzt Frischfleisch-Rezepte (`containsFreshMeat`) von shelf-stable/veggie. Plan-Pool wird je Kategorie in `meat`/`nonMeat` gesplittet. Cluster-Länge `meatClusterDays(fridge, groupF) = floor(fridgeMeatCapacity / (groupF × 500mL))` geclamped [1, 6] — adaptiv pro Appetit über `groupFactor` (Light bekommt mehr Tage, Heavy weniger; Custom-kcal automatisch über `personFactor`). Post-Cairns- + Post-Bamaga-Cluster via `meatDayIndex`. Innerhalb Cluster sortiert nach Shelf-Life: Tag 0–1 short (Geflügel/Hack), Tag 2–3 medium (Wurst), Tag 4+ long (Beef/Lamm/Bacon) — `shelfPreference(clusterIdx)` definiert Tier-Reihenfolge. Frühstück+Lunch erlauben max 1 Fleisch je Cluster (Cluster-Tag 0 bzw. 0–1). Outside-Cluster + alle Off-Cluster-Tage werden vegetarisch/vegan, auch bei Omnivore. Optional-Zutaten (`Optional: bacon` in f1) und shelf-stable (canned tuna in m3, beef jerky in m9, „Chicken (leftover or canned)" in m1) zählen nicht als Frischfleisch. Neue Warnings: Fridge-Cluster-Info (small/medium fridge erklärt User die Cluster-Länge) + Few-meat-free-options-Hint. Smoke-Test 8 Plan-Konfigs + 13 Detection-Cases grün. Build 225.59 kB JS / 72.20 kB gzip · CSS unverändert.
- ✓ **Privacy Policy auf GitHub Pages gehostet** (2026-05-03) — Letzter Pre-Submission-Block für Google Play geschlossen. `scripts/build-privacy.mjs` (neu) liest PRIVACY.md, konvertiert via `marked` (neue devDep, ~50 kB als devDep) zu HTML, wraps in standalone HTML-Template mit minimal inline-CSS (Cape-York-Orange-Akzent, mobile-first responsive, max-width 720px für Tablet/Desktop-Lesbarkeit, ohne externe Dependencies — wichtig damit das File aus jedem Browser stand-alone rendert). Hookt sich an `npm` standard `prebuild`-Hook → läuft automatisch vor jedem `npm run build` (also auch im CI-Deploy). `public/privacy.html` ist gitignored (auto-generiert, Source of Truth bleibt PRIVACY.md), wird vor Build frisch generiert. Vite kopiert public/* → dist/ automatisch → GitHub-Pages-Deploy serviert das File unter **https://japeyer.github.io/cape-york-app/privacy.html**. Diese URL ist Play-Store-tauglich: standalone, ohne Login-Wall, stabil über Releases (auto-generiert aus dem versionierten PRIVACY.md), HTTPS. AboutTab-Link aktualisiert: `S.about.privacyUrl` von `github.com/.../blob/main/PRIVACY.md` auf die Pages-URL umgestellt. Wartungs-Reminder als HTML-Kommentar oben in PRIVACY.md hinterlegt: bei Änderungen NICHT die HTML-Variante editieren — MD-File ändern und build laufen lassen. Bundle unverändert (privacy.html ist statisches File, nicht im JS-Bundle), Tests bleiben bei 141 grün.
- ✓ **Pre-Play-Store-Block: Privacy + About-Page** (2026-05-03) — Erster Schritt Richtung Play-Store-Submission. **Audit der Code-Base**: `grep` über `src/` nach `localStorage`/`fetch`/`XMLHttpRequest`/`WebSocket`/`navigator.send` + manuelle Prüfung von `index.html` (kein externes Asset) + `vite.config.js` (Service Worker) + alle Production-Dependencies in `package.json`. Ergebnis: **5 localStorage-Key-Patterns** (`cfg_v1`, `ck_<id>`, `del_<id>`, `qty_<id>`, `add_<bucket>_<id>`), **0 Network-Calls in src/, 0 Cookies, 0 Tracker, 0 Drittanbieter-CDNs**. `workbox-google-analytics` ist transitive Dependency aber nicht aktiviert (verifiziert via `dist/sw.js` + `dist/workbox-*.js` Inspektion). **`PRIVACY.md`** (neu, ~120 Zeilen) als user-facing Privacy Policy in Englisch. **`DATA_SAFETY.md`** (neu) als interne Audit-Doku mit vorbereiteten Play-Store-Form-Antworten + vollständigem Daten-Inventar + Compliance-Reminder bei zukünftigen Code-Änderungen. **`AboutTab.jsx`** (neu) als neuer View in der State-Machine (`view='about'`) — erreichbar via **ⓘ-Icon im Topbar** (nur sichtbar auf Home, sonst Topbar überladen mit Back-Button gleichzeitig). 5 Sektionen: App-Header (Icon + Name + Tagline), „Your data" mit Link zur PRIVACY.md auf GitHub, „Data sources" (zeigt OSM-Snapshot-Datum dynamisch via `ROUTE_POIS_GENERATED_AT` + ODbL-Attribution global statt nur kontextuell im DaySheet), „Important" (Disclaimer mit Orange-Akzent — planning aid, OSM-Daten können outdated sein, Allergen-Filter ist Heuristik), „Contact" (Email + GitHub-Source-Link). Strings in `S.about.*` (10 Keys), CSS `.about-*` (~80 Zeilen). View-State erweitert: `home/about/trip-config/trip-active`. Topbar-Logik: Title wechselt zu „Cape York 2026" auf About, Subtitle leer. **Pre-Submission-Reminder**: vor Play-Store-Listing muss `PRIVACY.md` als HTML auf einer stabilen GitHub-Pages-URL gehostet werden (Google verlangt Privacy-URL ohne Login-Wall — GitHub-Repo-URL geht für jetzt, aber langfristig braucht's eine `/privacy.html`). Bundle: 270.28 → 273.45 kB JS (+3.2 kB, +1 kB gzip), CSS 24.46 → 25.74 kB. Tests bleiben bei 141.
- ✓ **Fuel-Section im DaySheet** (2026-05-03) — OSM-Daten erstmals UI-sichtbar. `src/lib/route-position.js` (neu) schätzt km-Position pro Trip-Tag via linearer Interpolation zwischen Cairns (km 0, Tag 1) und Bamaga (km 1000, bamagaDay), plus Richtungs-Flag (north/south); ohne `bamagaStop` liefert estimateRoutePosition `null` → Section versteckt. `nextFuelStops(km, dir, count)` filtert FUEL_STOPS nach Richtung + zählt Bamaga-BP nicht als „nach Bamaga" (strikt > km). (unnamed fuel) ohne Brand werden ausgefiltert (mostly Cairns-Metro-OSM-Lücken). DaySheet-Erweiterung: neue Section unter Restaurants mit Hint („Estimated position: ~500km from Cairns, heading north"), Card pro Stop mit Name + „Brand · km X" + ⛽-Diesel-Badge wenn OSM-tagged, ODbL-Attribution kleingedruckt unten. ConfiguratorTab gibt `trip={days, bamagaStop, bamagaDay}` durch. CSS: `.sheet-fuel-list/-row/-name/-meta/-diesel/-empty/-attribution` (~50 Zeilen). 16 neue Tests in `route-position.test.js` (Outbound/Return-Interpolation, Edge-Cases nicht-finite/out-of-range/no-bamaga-stop, nextFuelStops Reise-Reihenfolge + count-Limit + strikte > km + (unnamed)-Filter). Bundle: 258.12 → 270.28 kB JS (+12 kB, +3.2 kB gzip — FUEL_STOPS sind jetzt nicht mehr tree-shaked weil `route-position.js` sie importiert). CSS 23.90 → 24.46 kB.
- ✓ **OSM-Fuel-Stop-Datenpipeline + Tests** (2026-05-03) — Cape-York-Trips sind tankplanungs-kritisch (längste Sprit-Lücke 189 km im Outback). `scripts/fetch-osm.mjs` (neu) hits Overpass API mit QL-Query auf `amenity=fuel` Nodes+Ways in BBox (-16.95/-10.55 lat × 142.0/145.85 lon), berechnet Distanz via Haversine × 1.30 Road-Factor (empirisch aus Cairns→Bamaga 770 km Luft / 1000 km Straße abgeleitet), schreibt `src/data/route-pois.js` mit 71 Stops + ODbL-Attribution + Generierungs-Datum. Verifikations-Tabelle gegen hartkodierte `kmFromCairns` in `regions.js` druckt Diff pro Supply-Point: Cairns 0/Coen -20/Archer -29/Bamaga -8 (alle bestätigt), Cooktown -108 ⚠ (Mulligan-Highway ist deutlich kurviger als die 1.30× Heuristik annimmt — 330 km bleibt korrekt). `npm run osm:refresh` Script in `package.json` für manuellen Re-Fetch (bewusst nicht im CI-Build, weil Build nicht fehlschlagen soll wenn Overpass down ist). 11 neue Tests in `route-pois.test.js`: Datenvertrag (alle Pflichtfelder, BBox-Coverage, Sortier-Invariante, ID-Eindeutigkeit), Plausibilität (≥30 Stops total, ≥5 nördlich Cooktown, ≥1 bei Bamaga), ODbL-Compliance (`ROUTE_POIS_ATTRIBUTION` non-empty + enthält „OpenStreetMap"). Tooling-Notiz: Overpass HTTP 406 beim ersten Run gefixt durch User-Agent header (Overpass ist strict mit identifying UAs). UI konsumiert die Daten noch nicht — Build-Output unverändert (tree-shaking).
- ✓ **Vitest 4→3 + CI Node 22 — Lockfile-Konflikt-Fix** (2026-05-03) — Initialer Vitest@4-Push schlug auf CI bei `npm ci` fehl, lokal OK. Diagnose über mehrere Iterationen (Node 22 bump, Lockfile-Regenerierung, Verbose-Logging via Commit-Comment-Posting per workflow-GH_TOKEN). Root-Cause: Vitest@4 zieht eigenen nested `vite@8.0.10` mit, der `esbuild@^0.27 || ^0.28` braucht. Project-vite@5 nutzt esbuild@0.21.5. Auf Windows ARM64 schreibt npm die platform-binaries für die 0.28er-Version nicht korrekt in die Lockfile (bekannter npm-Bug, siehe Tech-Notizen unten) → 27 Pakete fehlen → `npm ci` (strict) bricht ab mit EUSAGE. **Fix:** Vitest auf 3.2.4 gepinnt — Vitest 3 hat `vite` als peer dependency, nicht als nested dep, nutzt also das vorhandene vite@5 → ein einziger esbuild im Tree. Workflow-Node bleibt 22 (matched lokale npm-11-Lockfile-Version). 114 Tests laufen weiter grün, Vitest-API zwischen 3 und 4 ist kompatibel — kein Test-Code-Anpassung nötig.
- ✓ **Vitest + 114 Tests für alle lib-Files** (2026-05-03) — Foundation für sicheres Refactoring vor dem Eigen-Trip + Pre-Play-Store-Pflichtblock. Vitest 4.1.5 als devDep, `npm test` und `npm run test:watch` Scripts in `package.json`. Test-Coverage: `lib/calories.js` (29 Tests: clampCustomKcal, personDailyKcal Standard+Custom, groupFactor mit allen Personen-Kombinationen, Migration), `lib/dates.js` (20 Tests: parseISO/isoFromDate-Roundtrip, addDays über Monats-/Jahres-/Schaltjahr-Wechsel, diffDays inkl. DST-Sicherheit, weekdayMo Mo=0..So=6), `lib/allergens.js` (26 Tests: Core-Hits, Topping-Markers, False-Positive-Filter für Plant-Milks/-Butters/`*-free`-Patterns, Multi-Allergen, Wortgrenzen), `lib/generator.js` (39 Tests: Output-Struktur, Input-Klemmung an System-Boundary, Determinismus, Bamaga-Routing inkl. clamping, Anti-Wiederholungs-Logik, Restaurant-Slots, User-Overrides mit Fallback bei invalid-id, Camping-Essentials-Skalierung mit Personen×Tagen+Fridge-Type, alle 5 Optional-Stop-Buckets, Allergen-Filter, Fridge-Cluster-Logik, Diät-Fallback, Item-ID-Stabilität für Storage-Keys). Tests laufen in **<400 ms** lokal. **CI integriert**: `.github/workflows/deploy.yml` ruft `npm test` vor `npm run build` auf — bei Test-Failure stoppt der Workflow und es wird nichts deployed. Bedeutet konkret: ein versehentlicher Refactor mit kaputtem Generator kommt nicht mehr ungefiltert in Production.
- ✓ **Codebase-Cleanup — Dead Code raus** (2026-05-03) — Drei Agents (`general-purpose`) parallel auf Dead-Exports / unbenutzte Strings / Dead-CSS angesetzt, plus manueller Sweep auf doku-only Daten. Gelöscht: `personFactor` `export`-Keyword in `calories.js` (nur intern benutzt), `S.app.subtitleEmpty` und `S.app.tabs.configurator` in `strings.js` (nirgends referenziert), die Felder `stores`, `capability`, `typicalDay` aus allen 5 supplyPoints in `regions.js` (`stores`/`capability` waren laut Header-Kommentar „rein dokumentarisch" für Stufe-2; `typicalDay` war Grundlage für `defaultDayForStop`, das beim Calendar-only-Stop-Refactor entfernt wurde). CLAUDE.md-Regel „don't design for hypothetical future requirements" angewendet — wenn Stufe 2 das Capability-Routing baut, kann es das auch frisch designen statt vorab geratene Strings zu erben. Gelaufen ohne behavior-Changes (Build grün, Bundle: 258.12 kB JS / 82.03 kB gzip — −1.2 kB JS, −0.28 kB gzip vs. vor Cleanup; CSS unverändert 23.90 kB). 0 unused CSS classes gefunden — Style-Cleanup war schon im letzten Commit erschöpfend.
- ✓ **Calendar-only Stop-Management — Stop-Pills raus, alles via Tap-on-Date** (2026-05-02) — UX-Vereinfachung: Stop-Aktivierung war doppelt verkabelt (PillPicker im Configurator + Tap im Calendar). Jetzt: Calendar ist die **einzige** Aktivierungs-UI. Bamaga-Yes/No-PillPicker + die 3 Optional-Stop-Pills (Cooktown/Coen/Archer) komplett raus aus `ConfiguratorTab`. `DaySheet` zeigt jetzt **alle 4** optionalen Stops als Toggle-Rows (statt nur enabled), mit drei Zuständen pro Stop: **inaktiv** → tap = enable + assign · **aktiv hier** → tap = disable · **aktiv anderswo** → tap = move (Label zeigt aktuellen Tag „currently day N — tap to move here"). `toggleStopForDay` macht alle drei Übergänge in einer Aktion. Tag 1 (Cairns-Start) und letzter Tag verstecken die Resupply-Section komplett — `allStopRows` returned `[]`. Neuer **First-Time-Tipp** als orange Hint-Box (`cfg-calendar-tip`) direkt unter dem Calendar erklärt das Tap-Verhalten + listet alle 4 Stops beim Namen damit User weiß was möglich ist. DaySheet bekommt zusätzlich eine kleine Section-Hint unter „Resupply stops" („Tap to mark this day as the arrival. Tap again to remove."). Datenmodell unverändert (`bamagaStop`/`bamagaDay`/`enabledStops`/`stopDays`) — Storage und Generator-API stehen, nur die UI-Affordance änderte sich. Build grün (259.33 kB JS / 82.31 kB gzip · CSS 23.90 kB / 4.72 kB gzip — ~14 kB JS- und CSS-Reduktion durch entfernte Komponenten/Strings/Styles).
- ✓ **Home-Screen + View-State-Machine — Trip „fixiert"** (2026-05-02) — Konzeptioneller Pivot in der UX: Trip-Planung war bisher direkt im Configurator-Tab; Tab-Klicks konnten beim Edit Plan/Listen versehentlich neu generieren. Jetzt: dreistufige View-State-Machine in `App.jsx` (`home` / `trip-config` / `trip-active`). **Home-View** (neu, `HomeTab.jsx`) ist der Einstieg — entweder Empty-State mit „Plan your Cape York trip"-CTA oder eine Trip-Card mit Trip-Name/Datums-Range/Summary + „Open"- und „Edit trip"-Buttons. Tap auf Card-Body → `trip-active` (Menu/Recipes/Shopping ohne Trip-Tab). „Edit trip" → `trip-config` (Configurator full-screen, ohne Bottom-Nav). „Reset & start a new trip" → Confirm + Voller Wipe + Onboarding. **Topbar** bekommt Back-Button (←) wenn nicht Home, sowie dynamischen Title („Cape York 2026" auf Home, „New trip"/„Edit trip" im Configurator, Trip-Name in Trip-Active). **Bottom-Nav** ist nur in `trip-active` sichtbar. CSS: `.app-no-nav .content { bottom: 0 }` damit Content scrollbar bleibt wenn Nav weg ist; neue `.home-*` Styles (Trip-Card mit Open-Button-Body + Edit-Action-Bar, Empty-State mit zentriertem CTA, Section-Label, Divider, Sekundär-Reset-Button mit Hint). Strings: neue `S.home.*` Section (subtitle, yourTripsLabel, tripDefaultName, openCta, editCta, createCta, createNewCta, createNewHint, createNewConfirm, emptyTitle, emptySub) plus `S.app.titleNewTrip` / `titleEditing` / `subEditing`. **Bewusst NICHT gemacht:** Multi-Trip-Support (nur 1 Trip pro `cfg_v1` — Listen-Optik in Home-Card antizipiert Stufe 2), Trip-Rename (`tripDefaultName` ist statisch, später als String-Input erweiterbar), Read-Only-Modus für Trip-Active (User kann immer noch Swap/Restaurant-Slots/Shopping editieren — der „definitive" Charakter bezieht sich auf die Config-Struktur, nicht jeden Plan-Slot).
- ✓ **Einkaufsliste editierbar — add/edit/delete pro Item** (2026-05-02) — User-Customization-Schicht über dem Generator-Output. Drei neue Storage-Prefixes: `del_<id>` (gelöschte Items, global pro Item-ID), `qty_<id>` (User-Override der Menge, global), `add_<prefix>_<id>` (per-bucket User-hinzugefügte Items mit `{name, qty, cat}`). ShoppingTab hat `applyCustomizations()` die diese Schicht auf die Plan-Daten legt; useMemo + version-bump triggert Re-Render bei jeder Änderung. Neue UI: ✎-Icon pro Item öffnet Edit-Sheet mit qty-input + delete-button (für user-added Items auch name editierbar). "+ Add item"-Button im jeden Cat-Header öffnet Add-Sheet mit name+qty input und cat-preset. "Add custom item to a new category"-Block am Listen-Ende um Items in nicht-existente Kategorien zu legen ("📦 Other"). Gelöschte Items zeigen eine "Hidden items"-Sektion am Listen-Ende mit Tap-to-Restore (↺-Icon). Customizations überleben Plan-Regeneration. Reset-all auf Trip-Seite ruft jetzt `resetAllShoppingState()` auf — räumt ck_/del_/qty_/add_ alles. Custom items haben "+"-Badge vor Name; qty-überschriebene Items haben kursive orange qty.
- ✓ **Stops im Calendar markieren — Multi-Stop-Day-Picker** (2026-05-02) — Bamaga-Day-Picker generalisiert auf alle aktivierten Stops. `stopDays: { cooktown: null, coen: null, archer: null }` neu in `cfg_v1`; null = nicht zugewiesen, sonst 1-basierter Tag wie `bamagaDay`. Beim Aktivieren eines Stops im Configurator wird `stopDays[id] = typicalDay` aus `regions.js` gesetzt (Cooktown→3, Coen→5, Archer→6). `regions.js` hat pro Stop ein `calColor`-Objekt (Cooktown blau, Coen violett, Archer rot-orange, Bamaga grün) — wird vom Calendar inline gerendert. `TripCalendar` von Bamaga-spezifischen Props auf generische `markedDays`-Liste umgestellt; `DaySheet` von `bamagaActive`/`onToggleBamaga` auf `enabledStopRows` + `onToggleStop(stopId)`. Tap auf Tag im Calendar → DaySheet zeigt einen Toggle pro aktiviertem Stop ("Set as Cooktown arrival day · ~330km from Cairns"). Stop-Pills im Configurator zeigen den zugewiesenen Tag als orange Badge ("Day 3"). Trip-Verkürzung clampt alle out-of-range Stop-Days auf null (force re-pick statt blind clamp). Generator unverändert — `stopDays` ist UI-only für jetzt; Stufe 2 könnte intelligentes Frisch-Routing basierend auf Stop-Days bauen.
- ✓ **Mehr Resupply-Stops — Cooktown, Coen, Archer River** (2026-05-02) — `regions.js` von 2 auf 5 Supply Points erweitert. Neue Stops sind alle `optional: true` und via Multi-Toggle im Configurator an/abschaltbar. Cape-York-Reise-Reihenfolge dokumentiert (km from Cairns + typicalDay) plus pro Stop ein `capability`-Objekt das beschreibt, was er pro Kategorie liefert (`full`/`good`/`limited`/`none`). Aktuell rein dokumentarisch — Generator routet weiterhin nur Cairns/Bamaga frisch (Stufe 2 kann das Capability-Objekt für intelligentes Routing nutzen). Cooktown ist `🏪` Last-Real-Supermarkt (~330km, IGA+Foodworks), Coen ist `⛽` mid-Cape (~580km, kleines IGA + UHT-Milch), Archer River ist `🏕` Roadhouse (~670km, Eis + Wasser + Snacks). Jeder enabledStop bekommt einen eigenen Tab + spezifische Essentials-Liste (Cooktown 6 items inkl. fresh bread; Coen 4 items; Archer 4 items inkl. cold drinks). `defaultConfig` erweitert um `enabledStops: { cooktown, coen, archer }` (alle false default), Migration mergt User-Wahl. App.jsx `buildTabs` filtert auf optional+enabled. Generator: neue Helpers `isStopEnabled` und `pruneDisabledStops` ersetzen den hartkodierten `delete shopping.bamaga`-Cleanup. Smoke-Test 5 Konfigs (none/all/single/no-bamaga-all) liefert konsistente Buckets. Bei allen Stops aktiv: 8 Tabs total — auf 360px Mobile noch lesbar (~45px/Tab), aber an der Grenze.
- ✓ **Camping-Essentials in der Einkaufsliste** (2026-05-02) — Architektur-Vorgabe 4 ("Versorgungspunkte datengetrieben") angewandt: pro `supplyPoint` in `regions.js` jetzt ein `essentials`-Array. Cairns: 15 Items (Wasser, Elektrolyt, Eis, Ziploc-Bags L+S, Foil, Cling film, Paper towels, Bin bags, Dish soap, Sponges, Matches, Oil spray, Wet wipes, Toilet paper). Bamaga: 4 Refill-Items (Wasser, Eis, Bin bags, Paper towels). `qty` ist String (fixe Mengen wie "1 large roll") oder Funktion mit Trip-Kontext (`{persons, days, fridgeSize, bamagaStop, bamagaActiveDay}`) — Wasser skaliert mit 3L/Person/Tag und splittet sich Cairns/Bamaga am Bamaga-Tag, Eis hängt an Fridge-Type (large=compressor → kein Eis nötig). Generator hat neue Funktion `injectEssentials` die nach `generateShopping` als eigene Kategorie "🧼 Camping essentials" oben in den jeweiligen Bucket geshovelt wird. ShoppingTab unverändert — rendert generisch über Kategorien. Bei `bamagaStop=false` wird der Bamaga-Bucket samt Essentials gelöscht (Cairns water deckt dann den ganzen Trip).
- ✓ **First-Open-Defaults + Reset-Buttons** (2026-05-02) — `defaultConfig()` in `useStorage.js` ist jetzt minimal: 7 Tage (Pool-Minimum), 2 Beispiel-Personen (zeigen die Add/Remove-Struktur), Bamaga-Stop **off**, keine Allergien/Overrides/Restaurant-Slots. Diet/Burners/Fridge brauchen Werte für die Pill-Pickers — bleiben bei sensible Defaults (omnivore / 2 burners / large). `defaultConfig` ist jetzt `export`-ed damit `App.jsx` für Reset wiederverwenden kann. Zwei neue Reset-Buttons: **„Reset all"** unten auf der Trip-Seite (rote Border, sekundär; nur sichtbar wenn `completed`) → `confirm()` → leert `cfg_v1` zurück auf Defaults und ruft `resetPrefix('')` auf (alle `ck_*` Shopping-Checkboxen weg). **„Reset all swaps (N)"** auf der Menu-Seite oben (orange Border; nur sichtbar wenn `overrideCount > 0`) → leert nur `cfg.overrides`, Restaurant-Slots und Settings bleiben. ConfiguratorTab bekommt `key={config.completed ? 'edit' : 'fresh'}` damit der lokale draft-State nach Reset frisch initialisiert wird.
- ✓ **Anti-Wiederholungs-Logik im Plan-Generator** (2026-05-02) — `pickMeat`/`pickNonMeat` in `src/lib/generator.js` vermeiden konsekutive Duplikate. Vorher: bei 1-Burner-Omnivore-Default fielen `shelfPreference(4)` und `shelfPreference(5)` beide auf `['long','medium','short']` und der `long`-Bucket enthielt nur 1 Rezept (a4 = Beef stir-fry) → Tage 5+6 und 13+14 waren beide a4. Jetzt: Closure-Variable `lastPick = {f,m,a}` trackt Vortags-Picks; Picker probieren bei Kollision die nächste Tier/Index-Option und consumen den ursprünglichen Counter nur dann, wenn keine Alternative existiert (Pool-Size = 1 → unvermeidbar). Funktioniert quer zu Restaurant-Slots (lastPick=null bei Restaurant → kein Sperre für nächsten Tag) und User-Overrides (Override setzt lastPick → Folgetag meidet die override-Wahl). Smoke-Test 8 Konfigs (1/2 burners × diet × fridge × Trip-Länge) zeigt 0 konsekutive Duplikate.
- ✓ **Allergien & Präferenzen-Filter** (2026-05-01) — Configurator hat neuen Block „Allergies or preferences? Yes/No" ganz unten; bei Yes erscheinen 8 Toggle-Pills (rote Akzentfarbe `#C92A2A`). Filter ist gruppen-weit, nicht pro Person. `lib/allergens.js` neu: pro Allergen Regex-Detection auf Zutaten, mit Topping- vs. Core-Klassifizierung und False-Positive-Filtern für Plant-Milks/Butters/`*-free`. Generator-Pool-Integration 2-stufig: core-Hits raus, topping-Hits ans Pool-Ende sortiert (Round-Robin pickt sie nur bei Wraparound = "wenn sonst wiederholen würden"). Pro Mahlzeit `meal.ta` → MenuTab/RecipesTab zeigen ⚠-Banner („Contains nuts as topping/optional — skip if allergic"). Toggle und Array entkoppelt im Storage damit Pill-Auswahl bei „No" erhalten bleibt. Smoke-Test 7 Plan-Konfigs + 12 Detection-Cases grün (incl. plant-milks/-butters nicht als dairy, „egg-free" nicht als egg, alle 3 pork-Dinners korrekt ausgeschlossen). Build 230.69 kB JS / 73.78 kB gzip · CSS 13.40 kB / 3.00 kB gzip.

### Morgen weiter mit (Stand Ende-Tag 2026-05-04)

**Heute durch:** Onboarding-Range-Select-UX. Live-Browser-Smoke-Test steht noch aus — Build/Tests sind grün, aber UI-Verhalten muss der Entwickler durchklicken (Range-Tap-Reihenfolge, Pending-Start-Highlight, Re-pick-Flow, Stop-Toggles bei kurzen Trips). Falls beim ersten Open ein alter `cfg_v1` mit `completed:true` lädt → Trip-Card erscheint statt Empty-State; das ist erwartet, „Reset & start a new trip" räumt es auf.

**Pre-Submission-Pflichtblock zu 100% durch.** Was noch zwischen jetzt und einer einreichbaren Android-APK steht — in der Reihenfolge in der morgen sinnvoll weiterzumachen ist:

**A) Echte Icons** *(klein, low-risk, OneDrive-OK, kein User-Logo-Design nötig)*
Aktuelle Icons sind reine orange Quadrate (`public/icon-{192,512}.png`). Pragmatischer erster Schritt: orange Quadrat + zentriertes 🦘-Emoji rendern (PowerShell + System.Drawing oder Canvas-API in einem kleinen Script). Pflicht für Play Store (sie verlangen 512×512 Hi-Res), aber für PWA reichen 192/512 in `/public`. Adaptive-Icons (foreground/background als getrennte Layer) brauchen wir erst nach Capacitor-Setup. **Empfehlung morgen anfangen**.

**B) OneDrive-Umzug — ✓ ERLEDIGT**
Repo liegt jetzt lokal unter `C:\Users\<user>\Projects\cape-york-app` (nicht mehr OneDrive). Der
Gradle-Sync-Konflikt-Blocker für Capacitor ist damit weg — C) kann direkt hier laufen.

**C) Capacitor-Setup → Android-APK** *(großer Code-Brocken, ~3-6 h, dedizierte Session)*
Nach Umzug: `npm install @capacitor/core @capacitor/cli @capacitor/android`, `npx cap init`, `npx cap add android`, Manifest minimal halten (nur `INTERNET`-Permission — siehe DATA_SAFETY.md § 5), Android Studio öffnen, Debug-APK bauen, auf physisches Samsung S installieren und testen.

**D) Play Console-Account** *(User-Aufgabe, $25 einmalig)*
Geht parallel zu C: https://play.google.com/console anmelden, Identitäts-Verifikation (1-3 Werktage), $25 Developer-Fee zahlen.

**E) Listing-Texte + Screenshots + Submission** *(nach C+D, ~1 Tag Arbeit)*
App-Description (kurz + lang), Screenshots in 4-5 Auflösungen, Privacy-URL eintragen (= `https://japeyer.github.io/cape-york-app/privacy.html`, schon hosted), Data-Safety-Form ausfüllen (Antworten in DATA_SAFETY.md vorbereitet), Internal Testing Track → Closed → Open → Production.

**Parallel laufende Themen die NICHT blockieren:**
- **Eigen-Trip Juni 2026 — ✓ ABSOLVIERT.** Der 16-Tage-Trip ist gefahren. Die Änderungen der letzten
  ~2 Wochen (Juli 2026) sind die **erfahrungsbasierten Anpassungen daraus** — u.a. Zutaten-Einheiten
  vereinheitlicht + can/gram-Bug (`ar`), geschätzte Kosten entfernt (`as`), Kochaufwand-Filter (`ap`),
  Onboarding-Tutorial (`aq`), Map fürs erste Release deaktiviert (`ao`), Schrift/Redesign. Damit ist
  das PRODUCT.md-§5.1-Gate (Praxis-Validierung vor Public Launch) inhaltlich erfüllt; eine formell
  ausformulierte Retrospektive (was lief / was fehlte / Stufe-2-Prioritäten) kann noch nachgezogen werden.
- **Monetarisierung** — Optionen besprochen (One-Time-Purchase favorisiert), Entscheidung nach dem Trip
  offen; für v1-Store-Launch wg. Play-Billing-Pflicht am einfachsten **kostenlos** starten.
- **Algorithm-Upgrade Stufe 2** — Trip-Feedback liegt jetzt vor (siehe Anpassungen oben).

**Erledigt heute (2026-05-04):**
- ✓ Generator-Klemmung [7, 28] → [1, 31] (Tagesausflug bis Monats-Trip)
- ✓ Storage Sentinel-Werte (days=0, startDate=null) für „kein Trip gewählt"
- ✓ TripCalendar Range-Select-Mode (1. Tap=Start, 2. Tap=End → days berechnet)
- ✓ ConfiguratorTab: Stepper raus, Days-Display + Re-pick-Button, Submit-Disable
- ✓ Stop-Filterung nach `minTripDays` (Cooktown=2, Coen=4, Archer=5, Bamaga=5)
- ✓ HomeTab + App.jsx Null-Handling für startDate=null/days=0
- ✓ 142 Tests grün, Build grün

**Bei Sessionbeginn morgen:**
- `npm run dev` für Dev-Server (vorheriger Hintergrundprozess beendet)
- Aktueller Branch: `main`, working tree dirty (Onboarding-Refactor noch ungommitted)
- 142 Tests grün

## Tech-Notizen für zukünftige Sessions

### Arbeitsverzeichnis
```
C:\Users\<user>\Projects\cape-york-app
```
Liegt **lokal, NICHT in OneDrive** (Umzug erledigt — s.u.). Pfad ohne Leerzeichen/Bindestriche.
Bash-Pfadform: `"/c/Users/<user>/Projects/cape-york-app"`.

### OneDrive-Umzug — ERLEDIGT
Das Repo wurde bereits von OneDrive nach `C:\Users\<user>\Projects\cape-york-app` (lokal) verschoben.
Der frühere „OneDrive-Sync-Risiko beim Capacitor-Setup"-Blocker ist damit **weg** — Capacitor/Android
kann direkt hier laufen, ohne Sync-Konflikte. (Der alte OneDrive-Ordner ist höchstens noch Backup.)

### Build-Befehle
```bash
npm install        # einmalig nach Clone / nach package.json-Änderung
npm run dev        # Dev-Server (HMR)
npm run build      # Production-Build → dist/
npm run preview    # dist/ lokal servieren
```

### PWA-Icons regenerieren („looks-real"-Platzhalter)
Wenn man die Platzhalter neu erzeugen will (z.B. andere Brand-Farbe), PowerShell.
Wichtig: `Font`-Konstruktor braucht `[single]`-Casts, sonst löst PowerShell die
Überladung nicht auf.
```powershell
Add-Type -AssemblyName System.Drawing
$dir = "C:\Users\<user>\Projects\cape-york-app\public"
$bg = [System.Drawing.ColorTranslator]::FromHtml("#C0600C")
$fg = [System.Drawing.Color]::White
foreach ($size in @(192, 512)) {
  $bmp = New-Object System.Drawing.Bitmap $size, $size
  $g = [System.Drawing.Graphics]::FromImage($bmp)
  $g.SmoothingMode = [System.Drawing.Drawing2D.SmoothingMode]::AntiAlias
  $g.TextRenderingHint = [System.Drawing.Text.TextRenderingHint]::AntiAlias
  $bgBrush = New-Object System.Drawing.SolidBrush $bg
  $g.FillRectangle($bgBrush, 0, 0, $size, $size)
  $borderPen = New-Object System.Drawing.Pen $fg, ([single]($size * 0.015))
  $inset = [single]($size * 0.10)
  $g.DrawEllipse($borderPen, $inset, $inset, $size - 2*$inset, $size - 2*$inset)
  $fgBrush = New-Object System.Drawing.SolidBrush $fg
  $fontCY = [System.Drawing.Font]::new("Arial", [single]($size * 0.30), [System.Drawing.FontStyle]::Bold)
  $cySize = $g.MeasureString("CY", $fontCY)
  $g.DrawString("CY", $fontCY, $fgBrush, [single](($size - $cySize.Width) / 2), [single]($size * 0.24))
  $linePen = New-Object System.Drawing.Pen $fg, ([single]($size * 0.012))
  $lineLen = [single]($size * 0.22)
  $g.DrawLine($linePen, [single](($size - $lineLen)/2), [single]($size * 0.58), [single](($size + $lineLen)/2), [single]($size * 0.58))
  $font26 = [System.Drawing.Font]::new("Arial", [single]($size * 0.13), [System.Drawing.FontStyle]::Bold)
  $size26 = $g.MeasureString("2026", $font26)
  $g.DrawString("2026", $font26, $fgBrush, [single](($size - $size26.Width) / 2), [single]($size * 0.62))
  $bgBrush.Dispose(); $fgBrush.Dispose(); $borderPen.Dispose(); $linePen.Dispose()
  $fontCY.Dispose(); $font26.Dispose(); $g.Dispose()
  $bmp.Save("$dir\icon-$size.png", [System.Drawing.Imaging.ImageFormat]::Png); $bmp.Dispose()
}
```

**Finales Logo einbauen:** das endgültige Design einfach als `public/icon-192.png`
und `public/icon-512.png` ablegen (gleiche Auflösungen, PNG-Format), `npm run build`,
fertig. Service-Worker invalidiert beim nächsten Page-Load automatisch den Cache.
